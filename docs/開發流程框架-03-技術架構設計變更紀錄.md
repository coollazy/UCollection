# UCollection 技術架構設計 — 版本異動歷程

本文件記錄 [開發流程框架-03-技術架構設計.md](開發流程框架-03-技術架構設計.md) 每個版本的完整討論脈絡與決策理由。技術架構設計本文僅保留一行摘要並連結回本文件對應版本，方便閱讀本文時不被冗長的歷史討論打斷。

---

<a id="v0-1"></a>
### v0.1（2026-09-01）

第1節整體架構總覽拍板：

- **Process 拓樸**：單一 Go binary，內部以常駐 goroutine 執行鏈上監控背景任務，不拆分多 process、不引入 message queue。理由：需求書為單租戶架構、無多副本/高可用要求，docker-compose 一鍵部署精神下越簡單越好。
- **docker-compose 服務組成**：`app`（Go binary）＋ `db`（PostgreSQL）兩個 container，不內建反向代理/TLS 終止（延續進度.md 已定案的技術選型）。
- **路由劃分**：單一 HTTP server、單一 port，依路徑前綴分流成 `/api/v1/...`（API Key+HMAC）、`/admin/...`（Session+2FA）、`/checkout/{orderId}`（無需登入）、`/tron-proxy/...`（同源代理，僅限已登入後台 session）四類。
- **組交易/廣播歸屬**：依 [驗證結論-08](驗證結論-08-嚴格CSP下組交易簽名廣播.md) 對階段03的提醒拍板，組交易與廣播兩個對外呼叫 TronGrid 的動作一律由後端經 `/tron-proxy` 執行，瀏覽器端只做衍生+簽名，不直連 TronGrid 網域，讓 CSP `connect-src 'self'` 可直接套用。
- **內部 package 劃分**：確立 `cmd/ucollection`、`internal/{config,store,hdwallet,order,scanner,tronclient,webhook,api,admin,checkout,consolidation,auth,audit}`、`web/static/js` 的 Go 慣用 layout，作為後續模組討論的檔案歸屬依據。

使用者確認方案可行，無調整。

<a id="v0-2"></a>
### v0.2（2026-09-01）

第2節資料庫 schema 拍板：

- **地址不獨立建表**：因「地址與訂單永久一對一綁定」且地址只在建立訂單當下才依索引衍生，`orders` 表直接帶 `master_wallet_id`／`derivation_index`／`address` 欄位，不另拆 `addresses` 表。
- **表格劃分**：`master_wallets`、`orders`、`order_state_transitions`（訂單完整狀態變更歷史，供5.2手動改判不覆蓋歷史用）、`incoming_transactions`（累加金額判斷用）、`consolidation_batches`/`consolidation_items`（手動歸集，逐筆獨立追蹤廣播結果）、`consolidation_address_book`（歸集地址簿，上限30筆）、`webhook_deliveries`、`api_keys`、`admin_account`、`audit_logs`、`system_params`，共11張表。
- **併發安全**：`master_wallets.last_derived_index` 用 PostgreSQL advisory lock 包住「讀取→+1→寫回→建立訂單」，確保高併發建單不會分配到重複 index。

使用者確認方案可行，無調整。

<a id="v0-3"></a>
### v0.3（2026-09-01）

第3節 HD 地址衍生模組拍板：

- **套件選型**：伺服器端只做「xpub → 地址」公鑰衍生，沿用驗證項目1（[評估-Go打包Docker可行性.md](評估-Go打包Docker可行性.md)）同一套組合：`btcsuite/btcd/btcutil/hdkeychain`（xpub解析與非硬化衍生）、`golang.org/x/crypto/sha3`（Keccak256）、stdlib `crypto/sha256`（checksum）、`btcsuite/btcd/btcutil/base58`（編碼）。
- **刻意不引入** `go-ethereum`／`gotron-sdk`／gRPC：那組依賴是驗證項目2簽名+廣播才需要的，伺服器端不簽名用不到，引入只會不必要拉大 image（純手刻11.3MB vs 含go-ethereum+gRPC 26.1MB）。
- **衍生邏輯**：xpub已編碼到account層級，伺服器再衍生兩層非硬化路徑 `xpub.Derive(0).Derive(index)`；衍生出的公鑰經Keccak256→取後20bytes→前綴0x41→雙SHA256 checksum→Base58編碼得到Tron地址；與第2節advisory lock銜接，分配index、算地址、寫入orders列在同一DB transaction內完成。

使用者確認方案可行，無調整。

<a id="v0-4"></a>
### v0.4（2026-09-01）

第4節鏈上監控模組拍板，過程經過完整討論修正與subagent兩輪審查：

- **原始設計的根本問題**：最初提案讓事件掃描/確認數計算迴圈依「是否有進行中訂單」啟停（回應使用者質疑「無訂單監控意義何在」的初步修正），但subagent第一輪審查指出這個方向本身是錯的——需求書5.3自己說事件掃描的API呼叫成本只跟區塊產生速度有關、不隨地址/訂單數量增加，為了省一個固定成本而啟停，換來一整組冷啟動/尾端漏記的窗口期風險（詳見審查發現1/2/3/6/f），不划算。
- **核心修正**：移除「監督迴圈+session啟停」機制，改為三個永遠常駐的背景任務：任務1（訂單生命週期巡查，純DB查詢）、任務2（事件掃描迴圈）、任務3（確認數計算+reorg檢查）。無進行中訂單時任務2降頻（非停止），checkpoint持續推進。
- **第二輪審查追加修正**：CONFIRMATION_STALLED判定比照EXPIRED加上「任務3已追上」守門條件（原本只修了EXPIRED這條路徑）；task3明確跳過已STALLED的訂單不自動轉回COMPLETED（呼應需求書5.2已定案的手動改判機制）；備援改用TronScan（不同資料源）而非TronGrid另一端點；金額運算全程用int64最小單位；`incoming_transactions`唯一鍵改用`(tx_hash, log_index)`；checkpoint/最新區塊高度查詢皆用`GREATEST()`保證單調；429/5xx指數退避重試；首次部署SOP需等第一筆checkpoint建立才開放接單。
- **明確記錄但不當作本模組bug的缺口**：COMPLETED後偵測到reorg的後續狀態機路徑，留給模組5定義；累加金額不分入帳來源，明講這是TRC20代收系統常態設計非疏漏；5.9「已歸集地址不再監控」與提前歸集進行中訂單的邊界情況，屬需求書措辭層級小缺口。
- **Gate**：驗證項目9結果出爐、checkpoint安全緩衝N值與reorg延伸緩衝期具體數字確定前，本模組不得視為可進入正式開發。

使用者確認方案可行，無調整。

<a id="v0-5"></a>
### v0.5（2026-09-01）

討論第4節鏈上監控模組時發現的邏輯漏洞（詳見 [需求書變更紀錄v0.36](開發流程框架-01-需求書變更紀錄.md#v0-36)）連動修訂：v0.4版「確認數達標後再加一段安全緩衝期才停止追蹤reorg」設計，把「商戶自訂確認數」跟「Tron協定自身solidified狀態」當成同一件事，但緩衝期該加多久講不出根據，正是v0.4版Gate卡住的其中一個卡點。拍板改用需求書v0.36已拿掉「確認數」商戶可調參數的方案，一路連動修訂：

- **第2節schema**：`orders`表參數快照欄位從四個減為三個，`confirmations_required`移除；`incoming_transactions`表`confirmations`（數字）欄位改為`confirmed`（boolean，`only_confirmed=true`查詢命中即true）；`system_params`表全域參數清單移除確認數。
- **第4節任務3**：更名為「最終確定（solidified）判定＋reorg檢查」，判斷邏輯從「自算目前區塊高度－交易區塊高度，跟商戶設定值比較」改為「直接以`only_confirmed=true`重新查詢，命中即觸發」；reorg檢查範圍限縮為「`only_confirmed=false`已命中、尚未查到`only_confirmed=true`」這段期間，查到`only_confirmed=true`後即停止追蹤，不再額外加緩衝期。
- **第4節Gate**：兩個卡點簡化為一個——「reorg延伸緩衝期具體數字」隨此決策消失，只剩「checkpoint安全緩衝N值」待驗證項目9完成後校準。
- **第4節「待模組5處理的缺口」**：「COMPLETED後偵測到reorg的後續狀態機路徑未定義」這項缺口一併消失（COMPLETED/OVERPAID現在只會在達Tron協定自身認定的不可逆狀態後才轉入，定義上不會再被reorg打回），標記刪除線保留記錄但註明已解決。
- **新增第5節「訂單狀態機模組」**：正式寫入模組5設計（先前僅為[階段03技術細節暫存.md](階段03技術細節暫存.md)草稿）。核心內容：`SELECT...FOR UPDATE`鎖定+Go端判斷的原子性做法；自動路徑（業務條件不成立則靜默ROLLBACK）與手動改判路徑（只檢查狀態白名單、不重查金額）共用同一骨架；容許誤差用預先算好的`amount_lower_bound`/`amount_upper_bound`兩個BIGINT快照欄位；狀態轉換判斷條件對應模組4任務2/任務3拆兩條查詢（PENDING→CONFIRMING只看下界、CONFIRMING→COMPLETED/OVERPAID看`confirmed=true`累加金額）；不再有COMPLETED/OVERPAID→CONFIRMATION_STALLED的reorg觸發邊，`decision_snapshot`/Webhook payload不需要`previous_status`/`reason`區分欄位。過程中一併套用審查發現的獨立修正：PENDING→CONFIRMING觸發條件原本寫「落在`[lower,upper]`區間才觸發」會導致超額付款卡死在PENDING直到轉EXPIRED（與需求書5.2 EXPIRED語意矛盾），修正為只看下界，上界判斷留給下一步分流。

使用者確認方案可行，無調整。

<a id="v0-6"></a>
### v0.6（2026-09-02）

[驗證項目9](驗證結論-09-TronGrid事件掃描保留期限與斷點續掃.md)結果PASS出爐，拍板第4節唯一剩下的Gate卡點——checkpoint安全緩衝N值：

- **發現原文件換算基準用錯**：v0.4/v0.5版寫「N值依驗證項目2量到的索引延遲上限（約60秒）換算」，但驗證項目2實測兩個不同延遲——`only_confirmed=false`約23.4秒、`only_confirmed=true`（solidified）約63.1秒。任務2的checkpoint推進用的正是`only_confirmed=false`查詢，該用23.4秒這個數字，原文件「約60秒」其實誤套用了任務3（確認延遲）的數字。
- **拍板換算**：Tron協定公開出塊時間約3秒/塊，23.4秒實測值（Shasta測試網單次觀察，非官方保證上限）套約2.5倍安全係數後取整數，**N = 20個區塊（≈60秒）**。使用者於三個選項（10/20/30個區塊）中選定20。選擇理由：N值大小對系統幾乎無代價——只影響EXPIRED/STALLED守門判定延後的秒數（相對訂單有效期通常是分鐘級可忽略），不影響金額判斷正確性（`(tx_hash, log_index)`唯一鍵已防重複），故沒有下修的理由。
- **新增防禦性降級告警機制**：依驗證結論09建議——「未證明永久保留，只是沒測到截斷」，補齊掃描連續多個歷史區間都回傳空陣列、且理論上該有資料時，觸發告警／人工複查，不直接信任空陣列＝沒事件，寫入任務2描述。
- **Gate解除**：第4節「最終確定判定」機制、第5節（依賴模組4`confirmed`欄位）皆可進入正式開發（階段05）。
- 同步修正：Process意外中斷重啟段落「停機期間TronGrid是否還查得到」不再標記「取決於驗證項目9（尚未執行）」，改為引用驗證項目9已確認的結論（回溯7.4年無保留期限截斷）。

使用者確認方案可行，無調整。

<a id="v0-7"></a>
### v0.7（2026-09-02）

第6節前台收銀頁模組拍板，經subagent兩輪審查修正：

- **草稿初版**：路由 `/checkout/{orderId}`，新增 `orders.public_token` 作為公開URL識別碼（不可用DB主鍵或`merchant_order_no`）、伺服器端QR code產生（僅放地址不編碼金額）、htmx輪詢刷新狀態、基本CSP baseline + noindex。
- **第一輪審查發現兩項高嚴重度問題**：(1) CONFIRMING狀態不受`expires_at`限制轉EXPIRED是模組5已定案規則，但草稿的前端倒數計時邏輯沒接住，會出現「倒數歸零但狀態仍顯示確認中」的矛盾畫面——跟模組4審查抓到的「CONFIRMATION_STALLED判定不對稱」同類問題，規則變了但下游沒同步調整；(2) htmx終態停止輪詢只寫「後端回應帶出信號」沒講具體手段，此端點無登入、無rate limit（丟給商戶反代層），若實作者做錯等於留下可累積資源消耗的漏洞。另有中/低嚴重度發現：路由參數命名`{orderId}`與`public_token`決策自相矛盾（需同步修訂第1節）、CONFIRMATION_STALLED文案被漏列、用戶端時鐘飄移風險、`public_token`未明訂需CSPRNG（`crypto/rand`）、缺Referrer-Policy、token唯一索引衝突未提及重試。
- **修正**：CONFIRMING狀態不顯示倒數、改顯示「確認中」不含時間壓力的狀態指示；終態停止輪詢明訂為「整段替換掉帶`hx-trigger`屬性的容器」；同步修訂第1節路由表`{orderId}`→`{token}`；CONFIRMATION_STALLED與EXPIRED文案方向分開列記；`public_token`明訂用`crypto/rand`產生32 bytes；加`Referrer-Policy: no-referrer`；補上token衝突retry要求。
- **第二輪審查**：確認上述9項修正皆為真實解決、非換句話說，但指出修正過程本身引入一項新缺口——「剩餘秒數」改為伺服器夾帶相對值後，前端如何呈現平滑倒數（純伺服器渲染會隨5秒輪詢間隔階梯式跳動 vs. 加一小段inline JS做每秒本地遞減+定期對齊）未定義。另兩項低嚴重度、非必要立即處理：初始頁面首次渲染若訂單當下已是終態，需直接輸出不含`hx-trigger`版本（自然推論但補上避免遺漏）；`remaining_seconds`到期瞬間與背景任務翻轉狀態間的~5秒既有時序窗口（系統既有特性延伸，非本模組新問題，僅記錄）。
- **最終拍板**：倒數計時採inline vanilla JS方案（`setInterval`每秒本地遞減、每次輪詢回應時用伺服器值重新對齊），不算違反「零Node.js建置鏈」原則（該原則限制建置工具鏈，非禁止少量inline JS）；一併補上初始渲染終態抑制的明文要求。
- **順帶修正模組2 schema既有缺口**：`amount_lower_bound`/`amount_upper_bound`欄位在v0.5新增第5節時已定案存在，但先前未補回第2節`orders`表格清單，本次一併補上。

使用者確認方案可行，但要求兩項調整：(1) 正式文件是給開發看的，兩輪審查的過程敘述對開發沒有幫助、只是雜訊，第6節本文開頭那段「本節經subagent兩輪審查修正...」整段拿掉，完整過程改為只保留在本變更紀錄（即本節）；(2) 「OVERPAID/EXPIRED是否要提示已收到部分/超額款項」這句帶著「列記待確認」字樣的TBD，不該留在正式架構文件裡（比照需求書不留待決狀態的原則），移到進度.md下一步清單第11項追蹤，第6節本文只保留「各狀態文案留待介面設計階段」這句範圍邊界說明。已依此調整。

<a id="v0-8"></a>
### v0.8（2026-09-02）

進度.md「剩餘模組拍板順序」第1項：新增第7節對外API模組（`internal/api`），對應需求書5.6：

- **路由**：`POST /api/v1/orders`（建單）、`GET /api/v1/orders/{id}`、`GET /api/v1/orders?merchant_order_no={xxx}`（查詢）。查詢僅支援單筆，不含列表/分頁——列表屬需求書5.5後台範疇，使用者於討論中確認先只限單筆。
- **認證機制討論**：草案提出API Key+HMAC簽章（Header：`X-API-Key`/`X-Timestamp`/`X-Signature`），但`api_keys`表原本欄位命名`secret_hash`與HMAC機制矛盾——HMAC驗簽是「伺服器用同一把secret重新計算簽章、比對計算結果」，不是單純比對雜湊值，雜湊過的secret無法逆推重算，伺服器必須持有明碼secret才能驗證。這跟密碼(password)驗證不同：密碼驗證是「使用者輸入雜湊後比對」不需要伺服器重算，可以只存雜湊；但HMAC需要重算，本質上做不到。此為HMAC機制本身限制，非設計疏漏，比照AWS/Stripe/GitHub Webhook業界慣例明碼存DB。討論中曾考慮應用層加密（用master key加密secret再入庫）增加防禦深度，但master key仍須放在伺服器啟動時可自動讀取的環境變數，形式意義大於實質防護，且與consolidation模組（人工每次輸入密碼解密助記詞）的加密情境本質不同，不套用同一套邏輯，故不加此層。**拍板**：`api_keys.secret_hash`更名為`secret`，明碼存，同步修正第2節schema表格。
- **建單API欄位討論**：草案僅定義`merchant_order_no`（必填）與`target_amount`（必填）兩欄位，對應需求書5.6明文範圍。曾討論是否比照Stripe新增選填`metadata`欄位（商戶自訂key-value，系統不解讀原樣回傳），若加則需額外決定格式限制、大小上限、以及metadata不同是否算冪等衝突（討論傾向metadata不參與衝突比對，以第一次存的為準）。**拍板不加**：需求書5.6沒有提出這個功能，屬猜測性擴充而非驗證過的真實需求，且商戶自行用`merchant_order_no`當key維護對照表成本很低，若之後商戶端真的反應需要，屬於需求書增修範疇，不在架構設計階段預先加。
- **冪等性衝突判斷討論**：使用者一度誤解「衝突比對用金額」是否代表「兩張不同訂單金額相同就會衝突」。釐清：衝突比對的觸發前提是「同一個`merchant_order_no`」，不是全域金額比對——查無此`merchant_order_no`一律正常建立新訂單，不論金額是否與其他訂單相同；只有查到同一個`merchant_order_no`已存在時，才會比對這次金額與原訂單是否一致，藉此判斷是「網路重試」（相同，回傳原訂單）還是「異常」（不同，409拒絕）。不同`merchant_order_no`的訂單完全互不影響。
- **回應/錯誤格式**：統一`{"error_code","message"}`結構；Rate limiting沿用第1/6節既有決策留給商戶反代層處理，應用層不重複設計。

使用者確認方案可行，無調整。

<a id="v0-9"></a>
### v0.9（2026-09-02）

進度.md「剩餘模組拍板順序」第2項：新增第8節Webhook通知模組（`internal/webhook`），對應需求書5.7，經subagent兩輪審查修正：

- **草稿初版**：transactional outbox模式（模組5終態轉換時同一transaction寫入`webhook_deliveries`，另有常駐worker輪詢實際發送HTTP，呼應第1節「不引入message queue」決策）、payload含四個event_type、沿用第7節HMAC設計但用獨立webhook secret、exponential backoff重試（30秒起跳/封頂1小時/最多10次）、手動重發更新同一筆記錄。
- **第一輪審查發現多項規格空白與矛盾**：(1) worker完全沒有重疊執行保護，滾動更新時新舊容器重疊可能對商戶推送同一event兩次，與模組4「部署層級保護」原則矛盾；(2) 只講重試，未定義at-least-once語意與商戶端幂等責任，process重啟可能造成HTTP已送達但DB未更新而重送；(3) `webhook_secret`的產生/測試/輪替完全沒設計，5.5明確要求「設定/測試」但草案沒回答；(4) 商戶尚未設定webhook URL時終態轉換要怎麼處理未定義（寫入會有「沒真的嘗試卻消耗attempt_count」的語意矛盾，不寫入則永遠無法補發）；(5) 手動重發「更新同一筆記錄」讓通知歷史失真，與需求書「商戶可自行排查串接問題」衝突；(6) backoff數值（30秒/1小時/10次）沒有理由說明，且總窗口只約4小時，商戶端中斷超過4小時系統會永久放棄且商戶不知情；(7) HMAC簽章body若存JSONB不保證保留原始序列化格式，重試時body可能不穩定；(8) 達重試上限後後台缺乏被動可見度，與模組4「儀表板可見度」先例不一致，手動重發功能形同虛設；(9) 模組5骨架未明講寫入outbox的歸屬位置，兩節缺乏交叉引用；(10)(11)(12) 手動重發失敗後續狀態、HTTP層細節（redirect/Content-Type/User-Agent）未定義、event_type範圍確認無過度設計。
- **第一輪修正**：拆`webhook_deliveries`（邏輯單位）+`webhook_delivery_attempts`（逐次嘗試明細，append-only）兩張表解決歷史失真；新增`event_id`供商戶去重並明講at-least-once契約；worker取件改用`SELECT...FOR UPDATE SKIP LOCKED`+立即UPDATE `status='sending'`；新增`awaiting_config`狀態處理未設定URL的邊界；`payload`欄位改TEXT於寫入當下固定，簽章一律發送當下即時運算；backoff總窗口從4小時延長為3天（對齊業界webhook慣例）；新增被動可見度Dashboard摘要；新增webhook_secret系統產生/TEST_PING測試按鈕設計；補HTTP細節。
- **第二輪審查**：確認9/12項問題已解決，但指出修正過程本身**新引入一項嚴重缺口**——`SELECT...FOR UPDATE SKIP LOCKED`+`UPDATE status='sending'`模式若process在HTTP完成前當機，記錄會永久卡在`sending`（不會被`WHERE status='pending'`查詢撈到），與自己宣稱的at-least-once契約直接矛盾（承諾至少一次，實際上event消失）。另指出6項待補：`event_id`產生時機/方式未定義（是否比照`public_token`應用層產生慣例）；`order_id`是否允許多筆（改判觸發二次通知）未在schema明講，容易被誤加唯一約束；手動重發是否影響`attempt_count`未定義，兩種答案都有副作用；`webhook_secret`「輪替」完全沒被回應；3天窗口跟v1的4小時一樣是憑感覺訂的數字，未標註為待拍板值；`system_params`把「訂單建立快照」與「發送當下讀最新值」兩種性質參數混在同一張表卻沒有在schema定位段落標註例外。
- **第二輪修正（未再開第三輪，範圍收斂直接補齊）**：worker查詢新增 `OR (status='sending' AND updated_at < now() - interval '2 minutes')` 逾時回收分支，解決卡死缺口；`event_id`明訂Go應用層`crypto/rand`產生比照`public_token`慣例；明講`order_id`無唯一約束；明訂手動重發不影響`attempt_count`與既有排程；`webhook_secret`輪替明訂無過渡期、後台需顯示警語；`system_params`新增欄位明講不比照本表快照慣例。
- **重試窗口最終拍板**：使用者選定**24小時**，取代草案的3天暫定值，其餘退避曲線（1分鐘起倍增、封頂6小時）維持不變。
- **同步修正**：第2節`webhook_deliveries`表格描述更新為新schema、新增`webhook_delivery_attempts`表列、`system_params`表格描述補充webhook欄位例外說明；第5節「回傳契約」段落新增一句交叉引用第8節的outbox寫入時機。

使用者確認方案可行，無調整（重試窗口按上述拍板為24小時）。

<a id="v0-10"></a>
### v0.10（2026-09-02）

進度.md「剩餘模組拍板順序」第3項：新增第9節帳號安全模組（`internal/auth`），對應需求書5.5「帳號安全」小節，經subagent兩輪審查修正：

- **草稿初版**：單一`admin_account`環境變數bootstrap、bcrypt密碼雜湊、密碼→TOTP兩階段登入（`pending_2fa`→`active`）、`admin_sessions`表存session、閒置30分鐘＋絕對12小時雙重逾時、`/tron-proxy`額外套用15分鐘TOTP新鮮度step-up驗證、IP層級登入限流、`SameSite=Strict`+Origin檢查CSRF防護。
- **第一輪審查發現4項高風險問題**：(1) `RequireSession`檢查邏輯只看兩個時間條件、完全沒檢查`status`欄位，`pending_2fa`（僅通過密碼、尚未完成TOTP）的session若直接打其他`/admin/...`路由會被誤判通過，等於2FA可被繞過；(2) 首次設定TOTP流程「產生secret→顯示QR→使用者確認」橫跨至少兩次HTTP請求，但草稿沒定義這個未確認secret的中繼儲存位置，流程實際上跑不起來；(3) step-up重驗證的「輕量步驟」沒有命名對應路由，導致它完全不在登入限流規則涵蓋範圍內，而它保護的正是`/tron-proxy`這個真正動用資金的高風險路徑；(4) 純IP-based限流可被輪換來源IP繞過，正好打在「密碼已外洩、只是卡在TOTP這關」這個2FA本該防禦的核心威脅模型上。另有5項中風險（需求書明文要求的「修改密碼」功能完全沒有流程定義；TOTP時間窗/時鐘漂移容忍度、重放防護皆未提及；CSRF比對的「部署網域」基準值沒有定義來源；step-up TOTP屬需求書未要求的新增機制，未同步回第1節路由表）與2項低風險（session cleanup機制顆粒度、`expires_at`欄位角色不清）。
- **第一輪修正**：`RequireSession`改為明確三條件（`status='active'`且未過期且30分鐘內活動）皆成立才放行；新增`admin_sessions.pending_totp_secret`欄位承接未確認secret，並用`UPDATE admin_account SET totp_secret=$1 WHERE totp_secret IS NULL`帶條件寫入處理並行搶先設定的競態；命名`POST /admin/reverify-totp`並納入限流端點清單；新增per-session層級`totp_attempt_count`累計失敗5次即整個session失效，作為IP限流之外可換IP繞過的第二層防禦；新增`POST /admin/password`完整規格（含要求再次輸入目前密碼、強制`RequireFreshTOTP`）；TOTP驗證參數補上±1期(90秒)時間窗與`admin_account.last_totp_step`重放防護；新增`PUBLIC_ORIGIN`環境變數作CSRF Origin比對基準、未設定則fail-fast；`expires_at`改為唯一過期判斷依據，不再與`created_at`動態推算並存；session cleanup改為搭載模組4任務1既有ticker。
- **第二輪審查**：確認前7項修正（含2項低風險）已紮實解決，但指出修正過程本身**新引入3項高風險與4項中風險**：(1) 文件宣稱「同步修訂第1節路由表」但實際去看第1節該列文字完全沒變，屬文件可信度層級問題；(2) 文件自稱「待使用者最終確認見文末問題清單」但全文件找不到這個清單，也沒被列進進度.md既有的待確認事項追蹤慣例，等於這個承諾被默默放掉；(3) `totp_attempt_count`欄位描述是「累計」而非「連續」，代表同一個12小時效期內的active session即使中間穿插多次成功操作，只要累計（不是連續）達5次失敗就會被整個砍掉，比文件行文給人的直覺印象誤傷機率更高；(4) `last_totp_step`是帳號層級單一值，若管理者同時開兩個分頁各自需要TOTP驗證、把手機上同一組當前碼分別貼進兩邊，較晚送達的合法請求會被誤判為重放而拒絕，文件完全沒討論這個真實會發生的假陽性情境；(5) per-session失效機制本身讓「只偷到session cookie、沒偷到TOTP裝置」的攻擊者能連續送5次錯誤reverify-totp強制登出管理者，形成騷擾但文件未提及評估；(6) cleanup搭載在模組4任務1的ticker，與該任務「訂單狀態機專屬職責邊界」的既有定位、以及第8節webhook「獨立worker」先例不一致，混淆了package職責分界；(7) `RequireFreshTOTP`的威脅模型邊界（防cookie外洩、不防瀏覽器本身淪陷）文件完全沒交代，容易被誤解為萬用防線。第二輪判斷為「尚不建議定案」。
- **第二輪修正**：第1節路由表`/tron-proxy/...`一列實際加註TOTP新鮮度要求；拿掉「待使用者確認見文末問題清單」的空頭承諾，改為直接拍板保留step-up機制並寫明理由（不留待決狀態）；`totp_attempt_count`改為「連續失敗、任一次驗證成功即歸零」語意，並同步修正欄位描述；TOTP重放防護新增「已知取捨」段落明講雙分頁同碼假陽性情境與其可接受理由（不計入嘗試次數、有明確錯誤訊息、發生機率低）；登入限流段落新增「已知限制」段落明講per-session機制的騷擾面與判斷可接受的理由（不構成實質存取權限風險）；session cleanup改為`internal/auth`自行開獨立ticker，不與模組4任務1混用；step-up段落新增「威脅模型邊界」一段，明講此機制防cookie外洩、不防瀏覽器淪陷。另有2項低風險（`pending_totp_secret`與`totp_secret`敏感度對等說明、`token_hash`碰撞機率不比照`public_token`設計retry）一併順手補上。
- **同步修正**：第1節路由表`/tron-proxy/...`驗證方式欄補上TOTP新鮮度要求。

使用者確認方案可行，無調整，明確同意本模組主動加上、超出需求書字面要求的兩項防禦深度設計：(1) `/tron-proxy` 額外要求15分鐘內TOTP新鮮度（step-up驗證）；(2) 同一session連續5次TOTP驗證失敗即強制登出（即使仍在12小時效期內）。

<a id="v0-11"></a>
### v0.11（2026-09-02）

進度.md「剩餘模組拍板順序」第4項：新增第10節手動歸集模組（`internal/consolidation`），對應需求書5.9，經subagent兩輪審查修正，另有2項經使用者確認：

- **草稿初版**：頁面權限分級（回應第9節留下的待辦，區分唯讀查詢與助記詞輸入/簽名/廣播兩類）；待歸集地址列表（選代收主錢包→即時查鏈上USDT餘額→只顯示餘額>0地址→COMPLETED訂單餘額超標另標示）；歸集地址簿CRUD（沿用階段03技術細節暫存.md的`tronWeb.isAddress()`格式驗證、Address Poisoning分段顯示防呆）；TRX手續費A1/A2（草稿版：A1以index 0地址為固定TRX來源）；助記詞輸入頁沿用驗證結論06/08已驗證的函式庫組合（`bip39`+`hdkey`+`@noble/curves`+`@noble/hashes`+`bs58check`）；組交易/廣播走同源代理`/tron-proxy/consolidation/...`（prepare→本地重算txID核對→簽名→反推地址自我核對→broadcast，逐步對應驗證結論06記錄的地雷：recovery byte佈局、`prehash:false`、fetch失敗不代表未廣播）；廣播結果依合約類型分流判斷（`TriggerSmartContract`看`receipt.result`、`TransferContract`看`blockNumber`，對應驗證結論06附加測試發現的差異）；IndexedDB本機加密儲存沿用驗證結論07的PBKDF2+AES-GCM技術棧。
- **第一輪審查發現的問題**：(1) 歸集地址簿的新增/改名/刪除原本只掛`RequireSession`（理由是不移動資金），但地址簿存在的目的正是讓商戶信任其中項目、降低核對戒心——若攻擊者僅偷到session cookie（未偷到2FA裝置，第9節既有威脅模型）就能預先寫入偽裝正常的惡意地址，屬於與「手誤」不同的威脅來源，步驟4的全字串核對防呆不必然攔得住；(2) lazy reconcile（廣播結果不確定時的鏈上狀態核對）原本綁定「開啟該批次結果頁」，語意模糊且商戶更可能只記得回來看列表頁，未必找得回舊批次頁面；(3) A1「代收主錢包內的TRX＝index 0地址」是本節自行解讀，經獨立評估後發現與需求書5.1「index 0系統不賦予任何用途、後台也不特別顯示」字面直接衝突（選用index 0等於賦予具體用途，且商戶需要知道其餘額才能操作，牽動是否顯示）；(4) 新發現的資金安全缺口：`prepare`查詢餘額受TronGrid帳戶餘額索引延遲（驗證結論06記錄約15秒）影響，若入帳剛好落在延遲窗口內，會用偏低舊值全額轉出，但廣播成功後`consolidation_status`立即永久鎖定退出監控，殘留差額之後不會被任何機制發現。
- **第一輪修正**：地址簿新增/改名/刪除升級為要求`RequireFreshTOTP(15分鐘)`，僅查詢維持`RequireSession`；lazy reconcile改為「載入`/admin/consolidation`列表頁時，一併查詢該代收主錢包底下所有broadcasting狀態項目」；A1的index 0解釋與新發現的索引延遲殘留風險皆列入待確認問題，不自行拍板。
- **第二輪審查發現的問題**：(1) 第2節schema表格原文從未真的同步修改，仍列著已廢除的`fee_source`欄位、缺`broadcasting`狀態，第10節文字聲稱「取代」但只是一句話帶過，對後續維護者不夠清楚；(2) lazy reconcile在商戶尚未選定代收主錢包時無`master_wallet_id`可收斂查詢範圍的情境未定義；(3) 助記詞/xpub比對僅在前端本地執行的資安疑慮：若被惡意瀏覽器擴充套件繞過，是否有其他機制兜底未交代清楚；(4) TRX儲值與USDT歸集兩組獨立批次間的時序依賴（子地址須先有TRX才能付歸集能量費）未明文定義系統是否把關；(5) `consolidation_items`/`fee_topup_items`允許同一`order_id`累積多筆列時，「查詢目前狀態」的方法未定義；(6) TRX手續費A2直接輸入私鑰缺少格式防呆，與代收主錢包助記詞格式驗證的對等要求不一致。
- **第二輪修正**：同步修正第2節schema表格本身（不只文字聲稱取代）並新增`fee_topup_batches`/`fee_topup_items`列；lazy reconcile補充「尚未選定代收主錢包時查詢範圍改為商戶名下所有代收主錢包」；助記詞比對段落補充「並非唯一防線」說明——即使前端比對被繞過，「簽名後自我核對」與Tron鏈上簽名驗證仍會攔截錯誤私鑰簽出的交易，資金安全邊界並非完全依賴前端比對；TRX儲值與USDT歸集的時序依賴明訂「系統不做順序把關，廣播失敗記錄`error_detail`、商戶重新發起即可」，延續既有「不做預先估算/預先檢查」設計哲學；補充「查詢某地址目前狀態一律取`created_at`最新一筆」的明文原則；A2私鑰輸入補上格式防呆（64碼合法十六進位、落在secp256k1合法範圍）。
- **待確認問題與使用者最終拍板**：
  1. **A1「代收主錢包內的TRX」該用哪個地址**——需求書5.9原文暗示要動用代收主錢包底下某個具體地址，但xpub本身無「餘額」概念不能直接收TRX。討論index 0方案（操作最簡但與需求書5.1「不作系統內部用途、後台也不特別顯示」字面衝突）與比照A2改為商戶自行指定地址兩個選項後，**使用者選定後者**：拍板不動用index 0，A1與A2一律採完全相同的「商戶指定地址+輸入助記詞或私鑰」機制，兩者在技術上完全一致，差異僅剩`fee_source`稽核欄位這個商戶自選的分類標記（A1＝商戶自認與代收主錢包相關的資金；A2＝完全獨立的錢包），系統操作流程與後端邏輯不做任何區分。
  2. **TronGrid索引延遲（~15秒）疊加`consolidation_status`永久鎖定的殘留金額風險**——若入帳剛好落在prepare查詢前的延遲窗口內，會用偏低舊值全額轉出，之後該地址永久退出監控，殘留差額不會再被任何機制發現或提醒（但仍安全存在鏈上，商戶可自行用助記詞重新衍生取回，非資金遺失）。討論「接受風險維持現狀」與「廣播成功後延遲30秒再查一次餘額才正式鎖定」兩個選項後，**使用者選定前者**：評估發生機率極低（需同時滿足入帳卡在延遲窗口內、商戶剛好此時歸集兩個巧合）、屬於需求書5.9本身已接受的「已完成歸集地址不再監控」既有限制的特例，不加二次覆核/緩衝等待機制，維持設計簡單性。
- **本機加密儲存PBKDF2迭代次數拍板600,000**：驗證結論07的300,000僅為測試參數，正式值依OWASP Password Storage Cheat Sheet對PBKDF2-HMAC-SHA256的建議下限直接定案，屬純技術安全參數、不涉及業務判斷，未列入待確認問題。
- **同步修正**：第2節`consolidation_batches`/`consolidation_items`表格描述更新（移除已廢除的`fee_source`、`broadcast_status`補上`broadcasting`中間態、新增`error_detail`），新增`fee_topup_batches`/`fee_topup_items`表列。

使用者確認方案可行，並針對兩項待確認問題完成拍板（見上方）。

<a id="v0-12"></a>
### v0.12（2026-09-02）

進度.md「剩餘模組拍板順序」第5項（最後一項）：新增第11節後台管理介面模組（`internal/admin`），對應需求書5.5，5個模組拍板順序全數完成，經subagent兩輪審查修正：

- **草稿初版**：路由總覽（儀表板/訂單列表明細/手動建單/手動重新查證/手動改判/代收主錢包設定/API Key管理/Webhook端點設定/參數設定/通知歷史/稽核日誌/CSV匯出）；權限分級沿用第10節唯讀vs寫入原則；代收主錢包導引Gate；儀表板三項統計定義；手動建單與第7節API共用create服務層；reverify沿用帳戶層級端點`/v1/accounts/{address}/transactions/trc20`；手動改判僅`RequireSession`；代收主錢包新增/恢復使用中沿用第10節離線bundle+嚴格CSP；API Key單一組+一次性明碼顯示；Webhook端點設定URL僅`RequireSession`、secret疊加`RequireFreshTOTP`；稽核日誌action_type清單；CSV/Excel匯出。
- **第一輪審查發現的問題**：高風險3項——(1) reverify改用的帳戶層級端點會撈到該地址收到的所有TRC20代幣、非僅USDT，未過濾合約位址則垃圾代幣入帳會被誤計入累加金額；(2) 該端點回應結構是否含有可對應`log_index`的欄位、與任務2/3合約事件端點是否語意一致從未驗證，貿然假設可共用同一去重鍵有風險；(3) 稽核日誌action_type清單自稱「彙整」卻漏了第9節`LOGOUT`、第4節`checkpoint異常倒退事件`、匯出、reverify四類操作。中風險5項——webhook URL變更權限判斷只論證外洩風險未論證竄改重導向風險；手動建單為已拍板第7節API新增`NO_ACTIVE_MASTER_WALLET`前提卻未正式標註為修正；手動建單金額換算未定義超過6位小數的處理；手動建單留空`merchant_order_no`若在送出當下才產生隨機碼，每次送出都不同、與第7節共用的冪等機制形同虛設；權限分級理由段落只講「寫入操作」判準，但表格裡`GET /admin/master-wallets/new`（唯讀頁面）也要求TOTP，論證與表格對不上。低風險5項——Gate標題「首次登入」與第9節用詞疊用；歸集地址簿被一併鎖進Gate範圍未說明理由；CSV/Excel匯出未討論大量資料處理；`reactivate`對「目標本已使用中」的邊界情況未定義；訂單明細頁未提及`fee_topup_items`。
- **第一輪修正**：reverify改用與任務2/3完全相同的合約事件端點（`GET /v1/contracts/{USDT合約}/events`），解決過濾與去重鍵相容性兩項高風險；稽核日誌action_type補上`SCAN_CHECKPOINT_ANOMALY`/`LOGOUT`/`EXPORT_ORDERS`/`ORDER_MANUAL_REVERIFY`；webhook URL變更升級為`RequireFreshTOTP`；新增「同步修正第7節」段落明確標註`NO_ACTIVE_MASTER_WALLET`錯誤碼；手動建單金額超過6位小數明訂拒絕送出；`merchant_order_no`自動編號改為表單載入當下產生、隱藏欄位固定送出；權限分級理由補上第二判準（高風險輸入頁面即使GET也納入）；Gate標題移除「首次登入」；明訂歸集地址簿不受Gate限制；CSV/Excel匯出加上串流寫入+10萬筆上限；`reactivate`對已使用中目標回傳明確錯誤；訂單明細頁補上`fee_topup_items`。
- **第二輪審查**：確認前11項修正皆為真實解決，僅webhook URL修正有殘留問題；另發現修正過程本身**新引入4項高中風險**：(1) webhook URL的M1修正只更新了路由表，下方「Webhook端點設定」小節的prose說明仍留著修正前「`RequireSession`，URL本身外洩風險有限」的舊理由，文件內部直接自相矛盾；(2) reverify查詢範圍縮限用「終態轉換時間」當查詢上界，但這與reverify存在的核心理由（地址永久有效、隨時可能收到終態後才延遲到帳的款項）直接矛盾——上界鎖在終態時間點，反而查不到reverify原本要服務的最典型情境；(3) reverify沿用合約事件端點後完全沒提及超過200筆時的`fingerprint`分頁機制，時間窗短不代表事件筆數少（鏈上全域轉帳量大時仍可能單頁超標）；(4) 手動改判（`override-status`）僅`RequireSession`，但模組5「手動改判路徑」設計本就不重查金額子查詢、無伺服器端金額防線，若session cookie外洩，攻擊者可直接把任一EXPIRED/STALLED訂單改判COMPLETED、觸發真實`ORDER_COMPLETED` Webhook，效果等同權限分級判準(1)所述的「偽造通知詐騙商戶」，與該判準自身邏輯矛盾。另有中風險2項：手動建單冪等修正只是前端表單行為假設，未定義非瀏覽器直接呼叫端點時的後端規則；`WEBHOOK_CONFIG_CHANGED`單一action_type未區分URL變更與secret輪替兩種風險層級不同的操作。第二輪通讀全文另發現1項獨立問題：儀表板「異常訂單數」排除全部EXPIRED的理由（「是否為終態」）與其對OVERPAID的認定標準（同為終態卻算異常）不一致，正確判準應是「是否需要商戶查證/處理」——EXPIRED裡「已收到部分款項但未達標」的子情況與OVERPAID/STALLED同樣資金卡在該地址需要商戶處理，不該整類排除。
- **第二輪修正**：webhook URL段落prose同步改寫，理由改為「竄改重導向」風險（靜默竊聽通知、商戶對帳靜默失聯）；reverify查詢上界改為一律用`now()`不分終態；reverify補上沿用任務2`fingerprint`分頁規則的明文要求；手動改判升級為`RequireFreshTOTP(15分鐘)`，權限分級理由新增判準(3)（無金額防線+可觸發真實終態Webhook）；手動建單明訂非瀏覽器呼叫時後端不強制`merchant_order_no`必填、留空一律視為新建（刻意簡化取捨，呼應本端點「方便測試」定位而非嚴格冪等對外API）；`WEBHOOK_CONFIG_CHANGED`拆分為`WEBHOOK_URL_CHANGED`／`WEBHOOK_SECRET_ROTATED`兩個獨立action_type；儀表板異常訂單數改為`OVERPAID + CONFIRMATION_STALLED + (EXPIRED且已入帳金額>0)`，判準改為「是否需要商戶查證」而非「是否為終態」。
- **同步修正第7節**：`RequireMasterWallet` Gate為建單API新增前提條件，錯誤碼清單補上`NO_ACTIVE_MASTER_WALLET`（HTTP 503）。
- 本節無新增/修正schema欄位，沿用第2節既有 `orders`／`api_keys`／`audit_logs`／`system_params`／`master_wallets` 定義。

使用者確認方案可行，無調整。

<a id="v0-13"></a>
### v0.13（2026-09-07）

階段07「全系統審查」（跳出個別模組、用CLAUDE.md安全鐵律/業務鐵律橫向核對所有模組）發現3項問題，皆已使用者拍板修正：

1. **`amount_tolerance_percent`缺RequireFreshTOTP保護**：`POST /admin/params`原本一次處理訂單有效期/金額容許誤差百分比/確認等待逾時時間三欄，整條路由只掛`RequireSession`。但金額容許誤差百分比一旦被調高，會讓之後所有新訂單的短付更容易被系統自動判定為COMPLETED——攻擊者僅取得session cookie（未取得2FA裝置）即可誤導商戶財務判斷，符合權限分級判準(3)「誤導商戶財務判斷」的精神卻未套用保護。**拍板**：拆出獨立路由`POST /admin/params/tolerance`疊加`RequireFreshTOTP`，其餘兩欄維持原路由`RequireSession`不變；權限分級理由新增判準(4)。已寫入第11節路由表、「參數設定」小節、「權限分級理由」段落。
2. **`master_wallets_new.go`的CSP漏了`frame-ancestors 'none'`**：該handler的`masterWalletNewCSPHeader`常數doc comment宣稱「逐字複製`internal/consolidation.signCSPHeader`」，但實際比對所有驗證結論文件記錄的基準CSP值，發現少了`frame-ancestors 'none'`這段clickjacking防護——這個頁面是CLAUDE.md安全鐵律3要求嚴格CSP的三個頁面之一（代收主錢包新增，輸入助記詞）。**拍板**：直接補上，純疏漏修正，不涉及設計取捨。
3. **業務鐵律7「已歸集地址不再監控」與階段06情境E後續修正衝突**：情境E後續修正（讓COMPLETED訂單晚到confirmed金額在dashboard可見）的判斷邏輯完全不分`consolidation_status`，導致已歸集地址收到晚到入帳也會被同一套機制標示異常——這與需求書v0.24「系統不主動偵測/通知」的舊決策直接衝突。**拍板**：推翻v0.24該部分，改為「已歸集地址晚到入帳一律標示異常供商戶查證，但系統仍不自動處置」，決策全文見[ADR-0015](adr/0015-已歸集地址晚到入帳可見度.md)。程式碼行為不需要改動，只修正文件與現況不一致的部分。已寫入5.9附近「已歸集地址不再監控」段落與殘留金額風險段落。

**同步修正**：需求書5.9（[v0.37](開發流程框架-01-需求書變更紀錄.md#v0-37)）、CLAUDE.md業務鐵律7與安全鐵律9範例清單。本次無新增/修正schema欄位。

使用者拍板：CSP與稽核紀錄缺漏直接修正；`amount_tolerance_percent`選擇拆分獨立路由（而非整頁疊加保護，或維持現狀接受風險）；業務鐵律7選擇推翻舊決策讓兩種情況都可見（而非修正情境E機制排除已歸集情況）。另有4項高風險寫入操作（歸集地址簿新增/改名/刪除、歸集批次/手續費批次建立）缺稽核紀錄一併於本輪修正，不影響本文件（純`internal/consolidation`程式碼補強，無架構/schema異動）。

**第二輪審查（同日）**：重新核對上述3項修正是否正確落地、以及修正本身有無引入新問題——確認CSP值/路由拆分/ADR交叉連結/稽核降級顯示皆正確，未發現功能性regression。但發現本節「`action_type`涵蓋範圍」清單（自稱「彙整...供實作對照」）未收錄本輪新增的6個action_type（`AMOUNT_TOLERANCE_CHANGED`、`ADDRESS_BOOK_ENTRY_CREATED`/`_RENAMED`/`_DELETED`、`CONSOLIDATION_BATCH_CREATED`、`FEE_TOPUP_BATCH_CREATED`）——純文件補齊，不涉及設計取捨，已直接補上。

<a id="v0-14"></a>
### v0.14（2026-09-07）

階段08前置：使用者在本機用貼近正式環境的docker-compose部署（接Shasta測試網）自行手動測試時發現：「金額容許誤差百分比」送出時，TOTP過期後導向重新驗證，驗證通過後跳出`405 Method Not Allowed`。

**根因**：`RequireFreshTOTP(15分鐘)`機制設計上，新鮮度過期時把被攔截的請求導向`/admin/reverify-totp`，驗證通過後用HTTP 303轉址「帶回」原本被攔截的操作——但303轉址不論原方法一律變成瀏覽器`GET`，若原始請求是只註冊`POST`的寫入端點，轉址回去就會撞405；且原始表單內容在第一次被攔截時就已遺失，即使方法對了也無法悄悄用舊資料重送，操作者必須重新填寫整張表單。這條路徑先前從未被端到端測過——過去所有手動測試操作都剛好在TOTP剛驗證完的15分鐘新鮮度視窗內完成。

**討論過程**：先修正「reverify完成後導回GET頁面而非POST端點本身」（避免405），但使用者實測後認為「驗證通過還要重新填一次表單」的UX不合理，追問「為什麼不直接把驗證碼欄位內建在原表單裡，同一次送出就完成」；進一步討論後，使用者反問「既然操作需要驗證碼，為什麼要用『距上次驗證是否超過15分鐘』這種時效判斷，而不是每次都要求輸入」。查證需求書5.5確認**15分鐘新鮮度窗口並非需求書規定，是ADR-0011在需求書字面之外主動加上的技術決策**，屬於可以重新評估的範圍。

**拍板**：依[ADR-0016](adr/0016-高風險操作每次要求TOTP不設新鮮度窗口.md)，取消15分鐘新鮮度窗口，改為`RequireTOTPCode`——高風險操作每次送出都要求當下輸入TOTP驗證碼，驗證碼欄位內建在觸發該操作的表單/請求本身裡，與其他欄位一起在同一次送出中驗證，驗證通過即同一次請求內直接完成，不需跳轉頁面；GET頁面（歸集簽名頁、代收主錢包新增頁、修改密碼頁）則每次造訪皆導向`/admin/reverify-totp`（不再有15分鐘免驗證的例外）。`admin_sessions.last_totp_verified_at`保留但改為純稽核用途，不再用於gating判斷。

**同步修正**：CLAUDE.md安全鐵律9、第9節「高風險操作Step-up驗證」本文與「修改密碼」小節、第10/11節所有路由表（`RequireFreshTOTP(15分鐘)`→`RequireTOTPCode`）、第11節「權限分級理由」判準用語。ADR-0011本文不刪改（其兩階段登入/暴力破解防禦部分維持有效），僅ADR-0016推翻其「15分鐘新鮮度窗口」部分。程式碼：`internal/auth/middleware.go`（核心機制）、`internal/admin`／`internal/consolidation`共13條路由改為內建驗證碼欄位。

使用者拍板：改成每次都要求輸入TOTP驗證碼（而非維持15分鐘新鮮度窗口）。

<a id="v0-15"></a>
### v0.15（2026-09-10）

第10節手動歸集模組流程重構：把「補 TRX 手續費」與「USDT 歸集」從兩組獨立操作，整合為商戶體驗上的單一流程。完整決策、被否決方案與同頁兩金鑰安全分析見 [ADR-0017](adr/0017-手動歸集流程重構自動編排與精省手續費.md)，需求書層面見 [需求書v0.38](開發流程框架-01-需求書變更紀錄.md#v0-38)。

**背景**：階段08驗收測試，商戶反映現行歸集對不熟加密貨幣者步驟過多、不友善。起於驗收問題第11項「手續費即時精算」，討論後擴大為整段重構。

**推翻的既有簡化取捨**（第10節）：
- 「系統不自動估算能量成本、僅靜態文案提示」→ 改為設定頁即時精算。
- 「TRX 儲值與 USDT 歸集不做順序把關、兩組獨立批次、無狀態機互鎖」→ 改為前端自動編排順序（後端仍逐筆獨立、不加互鎖）。

**第10節同步修正**：
- 「TRX 手續費」段落改寫為「TRX 手續費與歸集的整合流程」：即時精算（`triggerconstantcontract` 取 `energy_used` × `getchainparameters` 取 `getEnergyFee`；首筆首次收款較高、其餘較低；緩衝 +10%、取整 0.1 TRX；商戶可覆寫）、轉入前以 `getaccount` 扣除子地址現有 TRX、自動編排（補 TRX→等上鏈→首筆 USDT 歸集啟用目的地→等上鏈→其餘）、失敗停下＋依鏈上現況可重入、簽名頁同時輸入兩把金鑰且簽完 TRX 即刻清除來源金鑰。
- 路由表新增三個唯讀端點：`POST /admin/consolidation/fee-estimate`、`/admin/consolidation/trx-balance`、`/tron-proxy/consolidation/transaction-info`（皆 `RequireSession`）；權限分級理由補說明。
- 「組交易與廣播」新增步驟 6「等上鏈確認」（前端輪詢 transaction-info，與 lazy reconcile 互補）。
- 「已知限制」原「廣播前不預先估算」條改寫為指向新精算設計。
- Schema 段落補「最小持久化、靠鏈上現況推導」：不新增流程表、不加首筆欄位，重入靠鏈上現況即時推導。

**連動修訂**：ADR-0017（新增）、ADR-0016（頂部加批次寬限窗針對此流程延長至 30 分鐘的註記）、CLAUDE.md 安全鐵律 9（補註 tron-proxy 批次寬限例外）、需求書 v0.38。

<a id="v0-16"></a>
### v0.16（2026-09-11）

第10節「頁面權限分級」路由表修正，完整決策與被否決方案見 [ADR-0018](adr/0018-歸集準備頁與簽名頁GET不再套用TOTP.md)（部分修訂ADR-0017）。

**背景**：使用者在8080實測Phase 4b（準備歸集→整合簽名頁）發現走一次流程最多要輸入TOTP驗證碼三次——`POST /admin/consolidation/prepare-flow`一次、`GET /admin/consolidation/sign`觸發的`/admin/reverify-totp`一次、簽名頁自己的驗證碼欄位一次——跟ADR-0017「整合流程精簡商戶操作」的目標互相矛盾。

**修正**：
- `POST /admin/consolidation/prepare-flow`、`GET /admin/consolidation/sign`兩列驗證方式改為單純`RequireSession`，不再疊加`RequireTOTPCode`。TOTP驗證收斂到簽名頁本身既有的驗證碼欄位，由第一次`/tron-proxy/.../prepare`呼叫消耗（`RequireTOTPCodeOrRecentStepUp`不變）。
- 「頁面權限分級」理由段落補充：這兩條路由例外的理由是兩者皆不移動資金，只建立/顯示待簽名batch內容；已與使用者說明「session cookie被偷可能被用來竄改待簽名batch目的地，需靠商戶簽名前核對頁面摘要」的殘留風險，使用者確認接受。
- 一併拿掉待歸集頁「每筆金額（TRX）」手動輸入欄位，改由`POST /admin/consolidation/prepare-flow`送出時自動呼叫既有的`fee-estimate`試算邏輯，算出首筆/其餘筆兩個金額帶去簽名頁（唯讀顯示）——ADR-0017決策3原文「此為輔助估算，商戶可於簽名頁覆寫」的「可覆寫」部分不再適用，商戶無法再手動修改試算結果，僅能核對。

**連動修訂**：ADR-0018（新增）、ADR-0017（頂部加部分修訂註記指向ADR-0018）、CLAUDE.md 不需修改（安全鐵律9原文未明列這兩條路由，屬模組內部細項）。程式碼：`internal/consolidation/{routes,batch_handlers,estimate,sign}.go`、`web/js-src/page.js`，見`docs/進度.md` 2026-09-11交接筆記。

<a id="v0-17"></a>
### v0.17（2026-09-11）

第2節 schema 表、第10節手動歸集模組、第11節 action_type 清單新增「手續費來源地址簿」（對應需求書 [v0.39](開發流程框架-01-需求書變更紀錄.md#v0-39)）。

**背景**：驗收問題第10項——手動歸集「TRX 來源地址」欄位是純文字框，商戶每次手動輸入自己固定的 TRX 錢包。使用者拍板做成與歸集目的地地址簿**分開的兩份獨立清單**。

**修正**：
- 第2節 schema 表新增 `fee_source_address_book`（`address` 唯一、`label`、`created_at`，結構同 `consolidation_address_book`）。
- 第10節新增「手續費來源地址簿」小節：獨立表、CRUD/上限/格式驗證/不影響歷史稽核**規則完全比照歸集地址簿**；補 TRX 廣播成功後自動存入手動輸入的新來源地址（best-effort side effect，比照 `AutoSaveIfNew`）；記錄「兩份獨立表」的分表理由（同 `fee_topup_batches`/`consolidation_batches` 判斷），不另開 ADR。
- 第10節路由表新增 `GET /admin/consolidation/fee-source-book`（`RequireSession`）與 `POST/DELETE`（`RequireSession` + `RequireTOTPCode`），權限比照歸集地址簿。
- 第11節 action_type 清單新增 `FEE_SOURCE_BOOK_ENTRY_CREATED/RENAMED/DELETED`。

**連動修訂**：需求書 v0.39。程式碼：新 migration `0009_fee_source_book`、`internal/consolidation/{fee_source_book,handlers_fee_source_book,routes,store,broadcast,handlers_pending}.go` 與模板。不涉及 CLAUDE.md 安全/業務鐵律。

<a id="v0-18"></a>
### v0.18（2026-09-11）

第6節前台收銀頁模組（`internal/checkout`）實作落地；同時修訂第6節「狀態更新機制」「倒數計時」「QR code」三處，讓文件對齊實際採用的技術。

**背景**：驗收問題第5項——`internal/checkout` 至今只有 `doc.go` 空殼（2026-09-02 模組拍板順序只排了 api/webhook/auth/consolidation/admin 五個，checkout 從未被排入），付款人無從看到收款地址/QR code。本次補齊。地基（`orders.public_token`、建單產 token、`skip2/go-qrcode` 依賴、`config.PublicOrigin`）先前皆已就緒，只缺模組本體。

**與原文的兩處刻意偏離（經使用者確認）**：
- **輪詢/倒數改用離線手寫 vanilla JS，取代原文的 htmx**：第6節原文（v0.7）寫「前端用 htmx 輪詢」，但 htmx 從未被 vendor 進專案，且 admin/consolidation 每一頁都刻意以手寫 vanilla JS（`localtime.js`/`admin.js`/`address-book-select.js`）完成、零 htmx 依賴。與使用者確認：兩者功能/安全無實質差別（收銀頁安全全來自伺服器端：`public_token` 高熵、壞 token 一律404不可探測、不外露容許誤差、`noindex`、`no-referrer`、baseline CSP），純工程取捨。拍板改用一支 `web/static/js/checkout.js` 同時管 5 秒 `fetch` 輪詢與每秒本地倒數，維持專案「零 htmx 依賴 + 手寫 vanilla」的實際慣例，CSP 得以維持 `script-src 'self'` 無 inline。
- **QR 改以 base64 `data:image/png` URI 內嵌**（原文只寫「Go 函式庫產生」未定交付方式）：與使用者確認採內嵌 data URI（不另設獨立圖片端點），只在初始頁面渲染。Go 端以 `template.URL` 帶入以繞過 html/template 對 `data:` scheme 的 URL 過濾（否則會被改寫成 `#ZgotmplZ`）。

**實作**：`internal/order` 新增 `GetByPublicToken`（mirror `GetByID`）；`internal/checkout` 新增 `deps/routes/templates/handlers.go` + `templates/{checkout_page,status_region}.html`（`GET /checkout/{token}` 完整頁 + `GET /checkout/{token}/status` 輪詢片段，皆無登入、`public_token` 為唯一存取控制）；狀態片段以 `data-terminal`/`data-countdown`/`data-remaining` 屬性驅動 `checkout.js`；狀態/倒數依 6 狀態分流（PENDING 才倒數、CONFIRMING 不倒數、四終態停輪詢、初始頁若已終態即不啟動）；baseline 安全 headers（CSP `img-src 'self' data:`、nosniff、no-referrer）。`main.go` 掛路由。**不涉及金鑰/簽名/廣播，不涉及 CLAUDE.md 安全/業務鐵律**。程式碼與測試見 `docs/進度.md` 2026-09-11 交接筆記。
