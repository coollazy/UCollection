# 驗證結論 10：階段06整合試跑

- 對應：[開發流程框架](開發流程框架.html) 階段06「整合試跑」
- 計畫全文：`.claude/plans/cuddly-snacking-orbit.md`
- 用途：本檔案是持續更新的測試案例追蹤表，隨測試進度即時填入PASS/FAIL，不是等全部做完才一次寫。

## Phase 3：主動情境測試（6項，已全數完成）

| # | 情境 | 狀態 | 備註 |
|---|---|---|---|
| 1 | 併發下單（30筆unique + 10筆重複merchant_order_no） | PASS | 地址/index零重複；重複訂單號正確收斂成同一筆 |
| 2 | TronGrid斷線 | PASS | scanner正確重試退避；dashboard正確顯示同步落後；reverify正確失敗並留稽核記錄 |
| 3 | TronGrid逾時 | PASS | 確認有client端逾時機制（`context deadline exceeded`），不會無限卡住 |
| 4 | TronGrid 429限流 | PASS | 正確辨識429（非誤判成其他錯誤），退避重試 |
| 5 | 復原能力（斷線恢復） | PASS | checkpoint正確追上斷線期間全部區間，零遺漏、免人工介入 |
| 6 | Race condition（手動改判vs scanner自動判定同時作用同一筆訂單） | PASS | 獨立harness直接呼叫`internal/order`正式函式製造真實併發（未修改scanner/order程式碼），100次迭代50/50平衡兩種贏家順序，零異常。測完已刪除harness、清空fixture資料，repo確認乾淨 |

## 訂單狀態機真實鏈上情境測試（12項，進行中）

用真實Shasta測試網轉帳，全程走**正常路徑**（後台/API建單，不繞過前台入口），讓scanner自然偵測，不用SQL/手動介入模擬入帳。

**付款方測試錢包**：原本計畫用index 0保留地址`TUEZSdKsoDHQMeZwihtdoBiN46zxhGWYdH`（業務鐵律5規定永遠不分配給任何訂單，理論上不會跟測試自身的訂單地址衝突）。**但實測中發現這個假設有漏洞**：這組助記詞（`abandon abandon...about`）是全世界最知名的公開BIP39測試助記詞，index 0又是最常見的預設衍生路徑，情境D測試途中（2026-09-05 16:55 UTC左右）該地址被不明第三方（很可能是自動掃地機器人）轉走餘額1660.322123 USDT——**不是我們系統的bug，是使用公開知名測試助記詞在公開測試網上的已知風險，測試網資金不受任何人獨占控制**。已改用同一助記詞的index 3地址（`TRhVWK5XEDkQBDevcdCWW7RW51aRncty4W`，餘額4785 USDT，實測時仍未被動用）續測，之後如果這個也被領走，需要換一個非公開助記詞衍生的測試錢包（或用faucet重新申請）。

測試金額設計：訂單金額100 USDT（`target_amount=100000000`，6位小數），`amount_tolerance_percent=0.5%`，故下界=99.5、上界=100.5 USDT。

| # | 情境 | 分類 | 狀態 | 備註 |
|---|---|---|---|---|
| A | 單筆精確轉到下界99.5 | 可直接測 | **PASS** | order 201：精確下界金額 → 10秒內CONFIRMING → 30秒後COMPLETED，驗證`>=`下界含 |
| A2 | 單筆精確轉到上界100.5（改用獨立新訂單測，見下方說明） | 可直接測 | **PASS** | order 202：精確上界金額 → CONFIRMING → 40秒後COMPLETED（非OVERPAID），驗證`>`上界不含 |
| B | 單筆轉超過上界1單位（改用獨立新訂單測） | 可直接測 | **PASS** | order 203：上界+1 → CONFIRMING → 60秒後OVERPAID，驗證真正超額才OVERPAID |
| C | 單筆不足（如50，不再補）→最終EXPIRED | 需等很久（現有設定30分鐘） | 未測試 | 驗證PENDING→EXPIRED，含「不足金額不會誤觸發CONFIRMING」（原C+F合併） |
| D | 兩筆分批湊到剛好100→COMPLETED | 可直接測 | **PASS** | order 204：先轉40（正確停留PENDING，未誤觸發CONFIRMING），再轉60湊到100 → CONFIRMING → 50秒後COMPLETED，驗證累計金額SUM()正確運作 |
| E | 先湊到100進CONFIRMING，之後意外多來一筆變超額 | 可直接測 | **FAIL（發現真實問題，已記錄，未修）** | 見下方詳細說明 |
| G | CONFIRMING卡太久沒最終確認→CONFIRMATION_STALLED | 需改設定較快驗證（現有設定1小時） | 未測試 | 驗證自動卡住判斷 |
| H | 承G，人工改判→COMPLETED | 依賴G | 未測試 | |
| I | 承G，人工改判→OVERPAID | 依賴G | 未測試 | |
| J | 承G，人工改判→EXPIRED | 依賴G | 未測試 | 需求書v0.33新增路徑（查證後確認根本沒收到錢） |
| K | 承C，人工改判→COMPLETED | 依賴C | 未測試 | 逾期後才發現有收到錢 |
| L | 承C，人工改判→OVERPAID | 依賴C | 未測試 | 逾期後才發現收到超額 |

**A2/B執行方式調整**：原計畫在同一筆訂單上依序補款（A→A2→B連續疊加），但A一達到下界就會在～30秒內被finality判定鎖定為COMPLETED終態，無法再疊加金額測試後續邊界——**改為A2、B各自用獨立新訂單、單筆一次到位精確金額**測試，效果相同（各自獨立驗證一個邊界點），只是不再是「同一筆訂單連續補款」的形式。

### 情境E詳細發現：晚到的confirmed金額不會讓已終結的訂單重新判定

**測試方式**：order 205先轉入精確100（觸發CONFIRMING），在CONFIRMING觸發後立刻（約10秒內）追加轉入20，測試finality判定是否會重新加總所有已確認入帳。

**實際結果**：
- 訂單最終卡在**COMPLETED**（`order_state_transitions`只有一筆PENDING→CONFIRMING→COMPLETED的紀錄，沒有第二次轉換）
- 但`incoming_transactions`真實加總（`detected_total`＝所有已偵測入帳，`confirmed_total`＝已最終確認入帳）最終雙雙來到**120000000**（120 USDT），超過上界100.5
- Webhook實際送出的payload：`{"status":"COMPLETED","target_amount":"100000000","total_confirmed_amount":"100000000",...}`——**回報給商戶的金額是錯的，少報了20 USDT**
- App log找到明確證據：`2026/09/06 00:56:56 scanner: finality task: handle event 90c53508...: order: current status not in allowed-from whitelist: order 205 is COMPLETED, allowed from [CONFIRMING]`——第二筆的最終確認事件因為訂單已經是COMPLETED終態而被系統**靜默拒絕**，只寫進app log（不是audit log、不會出現在後台任何畫面、不會觸發任何提醒）

**根本原因**：`internal/scanner/task_finality.go`的`scanFinalityOnce`用迴圈逐一處理每個鏈上事件，每個事件各自「先標記confirmed=true、再呼叫`EvaluateFinal`判定」，不是「先把這批事件全部標記完、才統一判定一次」。如果同一筆訂單有兩筆分開的入帳，且第二筆的最終確認（`only_confirmed=true`）比第一筆的判定時間點晚（即使只晚幾秒），第一筆就會先把訂單鎖定為COMPLETED終態；第二筆之後才確認時，`EvaluateFinal`因為訂單已經不是CONFIRMING狀態而被白名單擋下，這個判定結果就永久遺失，不會重新評估、也不會有任何人看得到這個落差。

**影響評估（僅記錄，不代表已拍板的修復方向）**：資金本身沒有損失（consolidation模組之後歸集是直接讀鏈上真實餘額，不是讀`orders`表的`confirmed`欄位），但**商戶端收到的Webhook與後台看到的訂單記錄會低報實際收到的金額**，且沒有任何機制能讓商戶或系統自己發現這個落差——除非事後手動比對鏈上餘額與訂單記錄。這符合CLAUDE.md「錯了會默默壞掉、不會報錯」的高風險模式，即使不在CLAUDE.md明列的金鑰/簽名/廣播範圍內，仍建議在Phase 5一併呈報給使用者決定是否要修。

**執行順序（使用者已確認）**：A/A2/B/D/E已完成，C/G（及依賴它們的H/I/J/K/L）等Phase 4（24小時被動觀察）跑完、確認系統本身穩定後再排。

**測試期間發現的問題一律先記錄在此表的備註欄，不立即修正程式碼**——等Phase 4穩定度測試整個結束後，再一併安排回頭處理，避免修改動作干擾正在進行的穩定度觀察。

## Phase 4：24小時被動觀察

進行中，見`docs/進度.md`交接筆記即時狀態。

## Phase 5：整理產出

待Phase 4與訂單狀態機情境測試都完成後，在此補上總結與階段06關卡是否滿足的結論。
