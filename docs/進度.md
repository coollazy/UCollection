# 專案進度

- 對應：[開發流程框架](開發流程框架.html) 的九階段
- 用途：記錄目前實際做到哪、下一步是什麼，取代把進度寫死在通用框架文件裡

---

## 目前位置：階段 01 已結案，階段 02 四度重新開放已完成（驗證項目9 PASS，九項全數收斂），階段 03 技術架構設計第1~9節已定案（第9節帳號安全模組剛拍板），依拍板順序繼續撰寫剩餘模組（consolidation起）

- **驗證項目9已完成，結論PASS**：查真實 Tron 主網 USDT 合約，`/v1/contracts/{合約}/events` 與 `/v1/accounts/{address}/transactions/trc20` 兩端點回溯測試皆未觀察到保留期限截斷（回溯至2019年合約/帳戶最早活動，約7.4年皆查得到資料），`fingerprint` 分頁在跨區塊中間切分的實測案例中與 ground truth 完全吻合、無重複無遺漏。詳見 [驗證結論-09-TronGrid事件掃描保留期限與斷點續掃.md](驗證結論-09-TronGrid事件掃描保留期限與斷點續掃.md)。
- **checkpoint安全緩衝N值已拍板＝20個區塊（≈60秒）**：驗證項目2實測`only_confirmed=false`延遲約23.4秒（任務2掃描用的正是這個查詢，原文件誤套用了`only_confirmed=true`約63.1秒的數字），套約2.5倍安全係數取整數得20個區塊。同時依驗證結論09建議新增防禦性降級告警機制。第4、5節Gate已解除，可進入正式開發。詳見 [開發流程框架-03-技術架構設計變更紀錄.md v0.6](開發流程框架-03-技術架構設計變更紀錄.md#v0-6)。
- 產出中：[開發流程框架-03-技術架構設計.md](開發流程框架-03-技術架構設計.md)（v0.10），逐模組討論拍板（沿用階段01做法），目前已定案：第1、2、3、4、5、6、7、8、9節，Gate全數解除。第4節「鏈上監控模組」、第6節「前台收銀頁模組」、第8節「Webhook通知模組」、第9節「帳號安全模組」皆經subagent兩輪審查修正定案，第5節「訂單狀態機模組」延續 confirmations_required 改版決策一併正式寫入，第7節「對外API模組」拍板`api_keys.secret`明碼存（HMAC機制限制）與冪等性判斷邏輯，第8節拍板transactional outbox模式、at-least-once語意、24小時重試窗口，第9節拍板密碼+TOTP兩階段登入、`admin_sessions`session模型、`/tron-proxy`額外15分鐘TOTP新鮮度step-up驗證、per-session連續失敗5次即登出+IP限流雙層暴力破解防禦（後兩項為需求書字面之外主動加上的防禦深度，已經使用者確認，詳見 [變更紀錄v0.10](開發流程框架-03-技術架構設計變更紀錄.md#v0-10)）。

### 剩餘模組拍板順序（2026-09-02 使用者確認，每次 /clear 後接續本清單繼續）

依重要性/依賴關係排序（api/webhook無依賴且是系統入口與終態通知，先清掉；auth雖小但admin與consolidation都要靠它的session模型，排第三；consolidation資金風險最高、技術最複雜，依賴到位後及早做，避免拖到最後才發現架構級問題；admin範圍最大、本質是整合前面所有模組的管理介面，放最後最順）：

1. [x] `api`（建單/查詢API，需求書5.6）——系統入口，無依賴 —— 已拍板，見 [v0.8](開發流程框架-03-技術架構設計變更紀錄.md#v0-8)
2. [x] `webhook`（Webhook通知，需求書5.7）——消費模組5終態結果，無依賴 —— 已拍板，見 [v0.9](開發流程框架-03-技術架構設計變更紀錄.md#v0-9)
3. [x] `auth`（帳號安全，需求書5.5其中一小節）——admin與consolidation的`/tron-proxy`皆為session-gated，需先定義 —— 已拍板，見 [v0.10](開發流程框架-03-技術架構設計變更紀錄.md#v0-10)
4. [ ] `consolidation`（手動歸集，需求書5.9）——資金風險最高，已有現成技術調查基礎（[階段03技術細節暫存.md](階段03技術細節暫存.md)的TronWeb地址驗證、驗證結論06/08簽名廣播實測經驗）
5. [ ] `admin`（後台管理介面，需求書5.5，範圍最大）——整合前面所有模組（訂單列表/代收主錢包設定/手動歸集操作UI/API Key管理/Webhook端點設定/參數設定/稽核匯出）

**未獨立排入章節、預期在寫上述模組時順便決定**：`audit`（稽核日誌，預期在admin時定案）、`tronclient`/`store`/`config`（共用底層基礎設施，預期在consolidation——第二個真正呼叫TronGrid的地方——時順便決定），若份量超出預期再回頭拆獨立章節。

**5個模組全部拍板完成後，階段03還沒結束**：官方定義階段03產出是**三樣東西**：架構文件（進行中）、專案層級 CLAUDE.md（給AI的行為準則，尚未開始）、決策紀錄 ADR（尚未開始）。階段03→04的關卡是「架構文件**與**CLAUDE.md已看過並定案」，不是架構文件寫完就算數。CLAUDE.md/ADR的材料已有部分累積在下方「待寫入未來CLAUDE.md／決策紀錄的筆記」段落（含xpub取得方式5個被否決方案的完整理由），5個模組寫完後需整理進正式CLAUDE.md/ADR檔案。
- **confirmations_required 改版已全數落實**：討論模組5（訂單狀態機）時發現模組4「確認數達標後緩衝期」設計有邏輯漏洞——**confirmations_required 從商戶可調參數改為固定沿用 Tron 協定 solidified 狀態（`only_confirmed=true`）**，已連動修訂完成：需求書 5.2/5.5（[v0.36](開發流程框架-01-需求書變更紀錄.md#v0-36)）、技術架構設計第2/4節與新增第5節（[v0.5](開發流程框架-03-技術架構設計變更紀錄.md#v0-5)）。完整決策脈絡見上述兩份變更紀錄，[階段03技術細節暫存.md](階段03技術細節暫存.md) 對應草稿已移除。

### 階段 01：需求定義 —— ✅ 已結案（v0.32，完整度審查全數收斂）

- 產出：[開發流程框架-01-需求書.md](開發流程框架-01-需求書.md)（完整版本異動歷程與每版拍板理由見 [開發流程框架-01-需求書變更紀錄.md](開發流程框架-01-需求書變更紀錄.md)）
- 唯一延伸到階段 02 的項目：完整度審查發現 M5（本機加密儲存 IndexedDB 從未被任何驗證項目觸碰）不屬需求書撰寫範疇，已轉為階段 02 驗證項目 7，已完成見下方

### 階段 01 需求書完整度再次審查（2026-09-01，四項缺口已全數拍板結案）

- 用途：重啟階段 03 前再次通讀需求書做完整度檢查，發現四項缺口皆未被過去 M1~M5、L1~L3 處理過，屬於新發現。四項已於同日全數拍板並寫入需求書，此處保留紀錄供之後回溯。
1. 訂單狀態機缺少「查證後確認沒收到錢」的結案路徑 —— 見 [v0.33](開發流程框架-01-需求書變更紀錄.md#v0-33)：`CONFIRMATION_STALLED` 新增可人工改判為 `EXPIRED` 的路徑，已寫入需求書 5.2、5.7。
2. 後台單一帳號的登入救援機制未定義 —— 見 [v0.34](開發流程框架-01-需求書變更紀錄.md#v0-34)：初始帳密由部署環境變數指定、後台提供修改密碼、不做應用層救援機制（遺失須自行資料庫層級重設），已寫入需求書 5.5、第7節。
3. 5.9 批次歸集「部分成功、部分失敗」未定義 —— 見 [v0.34](開發流程框架-01-需求書變更紀錄.md#v0-34)：逐筆獨立追蹤廣播結果，失敗筆可單獨重試、不影響已成功筆，已寫入需求書 5.9 流程步驟 6。
4. 「本機加密儲存」助記詞與多組代收主錢包的對應規則未定義 —— 見 [v0.34](開發流程框架-01-需求書變更紀錄.md#v0-34)：以代收主錢包識別碼/xpub 當索引鍵分開儲存，已寫入需求書 5.9（助記詞是否留存於瀏覽器）。

### 階段 02：技術可行性驗證 —— 四度重新開放已完成，九項全數 PASS

- **四度重新開放（已完成）**：階段03討論「鏈上監控模組」時發現，原設計假設「process重啟/長時間停機後能用checkpoint從任意久以前補齊漏掃事件」，這個假設從未被驗證過（驗證項目2只測過即時查詢剛發生的交易，沒測過查詢很久以前的區塊範圍是否仍有資料，也沒測過單次結果超過200筆時`fingerprint`分頁是否正確銜接）。若TronGrid對歷史事件查詢有未公開的保留期限，超過期限會查不到資料且API不報錯，屬於「補不回來、還不會報錯」的靜默漏收款風險。**新增驗證項目9**：查證 `/v1/contracts/{合約}/events` 端點對不同歷史區間（7天/30天/90天/1年/更久）的查詢結果、`fingerprint`分頁在超過200筆時的銜接正確性、`/v1/accounts/{address}/transactions/trc20`端點的保留範圍是否比contract-wide events寬鬆。定義見 [開發流程框架-02-技術可行性驗證.md](開發流程框架-02-技術可行性驗證.md) 驗證項目9。**結論：PASS**——查真實Tron主網USDT合約，兩端點回溯至2019年合約/帳戶最早活動（約7.4年）皆未觀察到保留期限截斷，`fingerprint`分頁跨區塊切分實測與ground truth完全吻合。詳見 [驗證結論-09-TronGrid事件掃描保留期限與斷點續掃.md](驗證結論-09-TronGrid事件掃描保留期限與斷點續掃.md)。
- **三度重新開放（已完成）**：需求書完整度回頭審查發現，驗證項目 6（組交易＋簽名＋廣播）從未套用過驗證項目 5/7 要求的嚴格 CSP（`script-src 'self'`、`connect-src 'self'`，無 `unsafe-eval`/`unsafe-inline`），但需求書 5.9「輸入助記詞頁面的強化要求」明文要求歸集簽名頁面（含 TRX 手續費 A1/A2 頁面）套用同等級 CSP。**新增驗證項目 8**：沿用驗證項目 6 完全相同的衍生/簽名程式碼（未重寫），改用同源代理架構（瀏覽器只 fetch 同源 `/tron-proxy/...`，由本機 server 在瀏覽器外部代為呼叫 TronGrid，貼近需求書 5.9 步驟 6 規定的架構）套用嚴格 CSP 重跑組交易＋簽名＋廣播流程。**結論：PASS**——功能結果與驗證項目 6 一致（簽名自我核對通過、廣播成功、鏈上 `receipt.result: SUCCESS`、獨立 curl 核對餘額變化吻合），12 則 console 訊息無任何 CSP violation，且結構性確認所有瀏覽器請求皆同源、無一筆直連外部網域。詳見 [驗證結論-08-嚴格CSP下組交易簽名廣播.md](驗證結論-08-嚴格CSP下組交易簽名廣播.md)。

- **重新開放原因**：5.9 v0.20 拍板 TRX 手續費機制為 A1（代收主錢包批次轉TRX）／A2（任意錢包批次轉TRX）。既有驗證項目 6 已驗證「瀏覽器端組交易＋簽名＋廣播」，但驗證對象是 **TRC20 代幣轉帳**（`TriggerSmartContract`，呼叫合約）；A1/A2 是 **TRX 原生轉帳**（`TransferContract`，不經合約），且皆為**批次多筆**。是否需要為此新增獨立驗證項目，經評估後拍板。
- **評估結論**：查證 [TRON 官方文件](https://developers.tron.network/docs/tron-protocol-transaction) 確認外層簽名信封（`raw_data → SHA256 → ECDSA簽名`）在所有 Contract 類型間統一，`TransferContract` 與已驗證的 `TriggerSmartContract` 差別只在 `parameter` 欄位內容，簽名/廣播機制無關；且 Tron **無帳戶 nonce**，批次多筆送出不需要序列相依（防重放靠 TAPOS：`ref_block`+`expiration`）；A2「任意錢包」只是換一個私鑰來源做同一件事，不構成新技術假設。核心機制風險已被驗證項目 6 完整覆蓋，**不開獨立驗證項目 7**，改為在驗證結論-06 補一段附加測試（實測 `TransferContract` 單筆＋批次連續送出 3 筆，Shasta 測試網），結論 **PASS**——三筆交易同一區塊上鏈、無互相干擾，付款方帳戶餘額扣款金額與預期完全吻合。唯一記錄的差異：`TransferContract` 的鏈上查詢回應沒有 `receipt.result` 欄位（該欄位是合約執行結果，原生轉帳不適用），階段 03 設計廣播結果判斷邏輯時需注意。詳見 [驗證結論-06-瀏覽器端TRC20轉帳組建簽名廣播.md](驗證結論-06-瀏覽器端TRC20轉帳組建簽名廣播.md) 「附加測試」章節。
- **再次重新開放（M5）**：階段01需求書完整度獨立審查發現，需求書5.9「本機加密儲存」（助記詞以自訂密碼加密存於瀏覽器IndexedDB）從未被任何驗證項目觸碰過，風險等級與已驗證項目4/5同類。**新增驗證項目7**（與上方「評估後確認不開獨立項目7」是不同的兩件事，此為需求書完整度審查後**新開**的項目，非同一次討論）：驗證PBKDF2+AES-GCM（瀏覽器原生WebCrypto，不需第三方函式庫）加解密助記詞、跨session存取IndexedDB、與既有驗證結論-01/03/04衍生結果比對、錯誤密碼應明確解密失敗、嚴格CSP相容性。**結論：PASS**，詳見 [驗證結論-07-本機加密儲存IndexedDB.md](驗證結論-07-本機加密儲存IndexedDB.md)。

對照 [開發流程框架-02-技術可行性驗證.md](開發流程框架-02-技術可行性驗證.md) 定義的驗證項目（項目 3 是回頭核對需求書後新增，5.9 手動歸集功能的關鍵假設；項目 4、5 是因 xpub 取得方式的需求缺口新增；項目 6 是討論 5.9 歸集流程時發現「瀏覽器端組交易＋簽名＋廣播執行」從未驗證過而新增；項目 7、8 皆為需求書完整度回頭審查發現的缺口，見上方說明）：

- [x] **驗證項目 1：HD 地址衍生正確性** —— **PASS**
  結論見 [驗證結論-01-HD地址衍生.md](驗證結論-01-HD地址衍生.md)
- [x] **驗證項目 2：TronGrid 測試網事件掃描** —— **PASS**
  結論見 [驗證結論-02-TronGrid事件掃描.md](驗證結論-02-TronGrid事件掃描.md)（Shasta 測試網，USDT 測試代幣轉帳，實測延遲：`only_confirmed=false` 約 23 秒、`=true` 約 63 秒）
- [x] **驗證項目 3：助記詞/私鑰硬化衍生正確性（手動歸集用）** —— **PASS**
  結論見 [驗證結論-03-助記詞私鑰硬化衍生.md](驗證結論-03-助記詞私鑰硬化衍生.md)（兩套獨立函式庫算出的私鑰/地址一致，且與驗證項目 1 的 xpub 衍生地址完全收斂，簽名驗證通過）
- [x] **驗證項目 4：瀏覽器端助記詞 → xpub 衍生正確性** —— **PASS**
  結論見 [驗證結論-04-瀏覽器端xpub衍生.md](驗證結論-04-瀏覽器端xpub衍生.md)（真實 Chrome 151 環境，兩套獨立函式庫組合（`bip39`+`hdkey`+`secp256k1` vs `@scure/bip39`+`@scure/bip32`+`@noble/curves`）算出的 xpub、地址與驗證結論-01/03 完全收斂；隨機助記詞交叉核對一致）
- [x] **驗證項目 5：xpub 衍生函式庫離線打包 + 嚴格 CSP 相容性** —— **PASS**
  結論見 [驗證結論-05-離線打包與嚴格CSP相容性.md](驗證結論-05-離線打包與嚴格CSP相容性.md)（`esbuild` 離線打包成單一靜態檔案，套用 `script-src 'self'`（無 `unsafe-eval`/`unsafe-inline`）嚴格 CSP，衍生結果不變、無 CSP violation；唯一需調整的是頁面不可用 inline `<script>`，需改外部檔案）
- [x] **驗證項目 6：瀏覽器端 TRC20 轉帳交易組建 + 簽名 + 測試網廣播執行** —— **PASS**
  結論見 [驗證結論-06-瀏覽器端TRC20轉帳組建簽名廣播.md](驗證結論-06-瀏覽器端TRC20轉帳組建簽名廣播.md)（Shasta 測試網，瀏覽器端手刻 ECDSA 簽名＋TronGrid 廣播，鏈上事件與雙方餘額變化皆核對一致；過程中踩到 recovery byte 佈局、`prehash` 預設值、廣播網路層失敗不代表交易未發生等地雷，已記錄供階段 03 手動歸集功能參考）
- [x] **驗證項目 7：本機加密儲存（助記詞加密存入 IndexedDB）正確性** —— **PASS**
  結論見 [驗證結論-07-本機加密儲存IndexedDB.md](驗證結論-07-本機加密儲存IndexedDB.md)（瀏覽器原生 WebCrypto，PBKDF2+AES-GCM，不需第三方函式庫；真實頁面 reload 模擬跨 session 讀取 IndexedDB，解密還原助記詞與原文逐字一致，重新衍生 xpub/地址與驗證結論-01/03/04 完全收斂，錯誤密碼觸發 `OperationError` 明確失敗，嚴格 CSP 下無 violation）
- [x] **驗證項目 8：手動歸集「組交易＋簽名＋廣播」程式碼在嚴格 CSP 下的相容性** —— **PASS**
  結論見 [驗證結論-08-嚴格CSP下組交易簽名廣播.md](驗證結論-08-嚴格CSP下組交易簽名廣播.md)（沿用驗證項目 6 程式碼不改動，改走同源代理架構套用嚴格 CSP，Shasta 測試網重跑組交易＋簽名＋廣播，功能結果與驗證項目 6 一致、鏈上獨立核對通過、無 CSP violation、無任何瀏覽器直連外部網域的請求）
- [x] **驗證項目 9：TronGrid 事件掃描資料保留期限與斷點續掃行為** —— **PASS**
  結論見 [驗證結論-09-TronGrid事件掃描保留期限與斷點續掃.md](驗證結論-09-TronGrid事件掃描保留期限與斷點續掃.md)（Tron主網真實USDT合約，回溯7.4年未觀察到保留期限截斷，`fingerprint`分頁跨區塊切分驗證無重複無遺漏；帳戶trc20交易端點保留範圍不比合約事件端點嚴格）

補充驗證（非原定清單內，但與階段 02 相關）：

- [補充] [Swift 打包 Docker 可行性評估](評估-Swift打包Docker可行性.md) —— 涵蓋驗證項目 1，技術上可行（PASS），但列出了三項尚待補驗的風險（amd64 架構、image 體積優化、其他第三方套件的 Linux 相容性）
- [補充] [Go 打包 Docker 可行性評估](評估-Go打包Docker可行性.md) —— **三項驗證項目全部用 Go 重做過**，皆技術上可行（PASS）。項目 1、3 第一次執行就通過（image 均 ~11MB、完全靜態連結）；項目 2 過程中發現一個真實的坑——簽名雜湊函式選錯（Tron 位址衍生用 Keccak256、但交易簽名雜湊其實是 SHA256，兩者容易混淆），修正後成功（image 26.1MB，因引入 go-ethereum + grpc 依賴）。amd64 架構三項都仍未驗證

### 階段 03 技術選型（已與使用者確認，不受本次新增驗證項目影響）

- **開發語言：Go**（三項可行性驗證用 Go 重做全部一次通過，image 最小、純靜態連結、無相容性問題；相較 Swift 踩了兩個 Linux 相容性坑、image 378MB）
- **資料庫：PostgreSQL**（比較 PostgreSQL／MySQL／SQLite 後定案，關鍵考量：地址分配的併發安全用 advisory lock 處理最直觀、JSONB 適合審計日誌明細、長期報表擴充性夠）
- **前端：Go `html/template` + htmx**（零 Node.js 建置鏈，僅高風險頁面單獨 vendor 對應函式庫離線打包）
- **不內建反向代理/自動 HTTPS**（假設商戶自行架設 nginx/Cloudflare 等前方 TLS 終止；系統只需監聽一般 HTTP port；未來要加不難，之後在 docker-compose 多掛一個 container 擋在前面即可，不需改動現有程式碼）

### 待寫入未來 CLAUDE.md／決策紀錄的筆記（尚未有檔案可放，先記在這裡避免遺失）

- **鏈上監控用語需修正**：之前草擬 CLAUDE.md 內容時寫成「禁止逐一輪詢地址列表」，範圍寫過頭了。需求書 5.3 原文是：此限制僅適用於**主要監控**（即時、持續、覆蓋所有進行中訂單），**備援/降級監控情境不在此限**（例如寬限期低頻監控、主要資料源掛掉切備援時），逐一查詢地址在這些情境下並非不可行。正確寫法應是「主要監控以事件掃描為主，備援/降級情境可接受輪詢」，不是絕對禁止。
- **xpub 取得方式，曾考慮但排除／未採用的做法**（供之後寫 ADR 時參考，避免之後重新討論一輪）：
  - 伺服器端一次性生成 xpub（後端生成助記詞、算完 xpub 就從記憶體清除、不落地）——**已排除**，因為即使不落地，助記詞仍短暫進過伺服器記憶體，違反「伺服器全程不接觸私鑰」原則，风险面比瀏覽器端生成大（crash dump、swap、日誌框架意外印出等）。
  - 商戶自行在既有錢包「新增帳戶」產生一批地址、匯出清單匯入系統（不透過 xpub／HD 即時衍生）——**考慮過但未採用**，可行但地址是有限批次不是無限衍生，商戶要記得補；並非本次選定的三種方式之一，如果之後三種方式在實務上卡關，這條路可以重新拿出來討論。
  - 固定地址 + 金額尾數區分訂單（放棄「每筆訂單獨立地址」）——**未採用**，直接牴觸需求書 5.1「地址與訂單永久一對一綁定」，屬於要重新打開需求書範圍的討論，不是單純技術選型。
  - 方式 1（商戶直接輸入既有 xpub）——**已廢除**（v0.5），原本列為三種取得方式之一並寫入 v0.3 需求書，後經市調（見下方市場調查筆記）發現市場上查無任何主流 Tron 錢包支援匯出帳戶層級 xpub，此選項實務上無法使用，予以移除。過程中衍生的「xpub有效性驗證」「與5.9歸集路徑一致性」等問題（問題清單第1、4點）隨方式1移除一併消失，不必再花力氣解。
  - 用智能合約（CREATE2/Forwarder模式）批次生成收款地址，取代xpub HD衍生——**評估後未採用**。討論5.1時發現PayRam的Tron/EVM鏈用的是智能合約架構而非xpub，一度考慮跟進評估。深入評估後結論：(1) 合約無法自己醒來執行歸集，需要一把**常駐operator私鑰**觸發sweep，私鑰曝險頻率比我們原規劃「助記詞僅5.9手動歸集時瀏覽器端短暫現形」更高，不是更貼近「伺服器全程不接觸私鑰」原則；(2) 歸集需額外一次「部署+自毀合約」開銷，成本比純位址衍生+TRC20轉帳更高；(3) 完全沒解決5.9歸集需要TRX/能量的問題，只是把簽名者從商戶助記詞換成常駐熱錢包；(4) 增加reentrancy、合約升級、稽核等純HD衍生不用面對的複雜度與風險。且查到業界逆向訊號：Kraken近期反而把EVM存款從智能合約地址改回標準EOA地址（[Kraken說明](https://support.kraken.com/articles/how-to-transition-to-unified-evm-deposit-methods-technical-overview-for-api-users)），方向相反。PayRam是閉源產品，合約細節無法查證，可能只是把類似HD邏輯包裝在合約UI底下，不代表業界公認更優解。技術參考：CREATE2在TVM上可行（[TVM vs EVM文件](https://developers.tron.network/docs/tvm-vs-evm)），Tron部署一個標準合約約消耗58~150 TRX（[Contract Development Cost](https://developers.tron.network/v3.7/docs/benchmark)），ERC-2802提案（[討論串](https://github.com/ethereum/EIPs/issues/2802)）明確指出sweep一定要外部帳戶發交易觸發。**維持xpub HD衍生路線**。
  - 商戶端「送出前驗證xpub是否正確」的UX機制（例如顯示地址預覽供商戶自行核對）——**評估後未採用**（問題清單第3點）。商戶現實中不會真的去核對，且若問題出在衍生邏輯本身有bug，商戶也無從比對出「正確答案」，加UX步驟未必有實質防護效果。改用工程手段：驗證項目4已用雙函式庫交叉比對確認結果一致（PASS），**階段04開發時須搭配自動化測試持續守住這件事**，不做成商戶端功能。

### 市場調查筆記（討論 5.1 時查證，供之後參考，避免重查）

- **xpub 衍生地址這個架構本身有市場先例，不是我們自己想像的做法，但 PayRam 對 Tron/EVM 鏈其實走的是不同技術路線，不是 xpub 衍生**：商業自架收款閘道 **PayRam**（UAE 公司，閉源）先前筆記寫「伺服器只握 xpub」，是根據其行銷文案「PayRam uses a read-only xPub」下的推論，經查官方 onboarding 文件（[Wallet Integration](https://docs.payram.com/onboarding-guide/wallet-integration)）後發現這句話**只適用於 Bitcoin**：Bitcoin 流程是商戶把 12 字助記詞貼進 PayRam 自己提供的工具算出 xpub（工具端「generate addresses」後助記詞即丟棄，貼近我們方式2/3 的精神，但流程是先在獨立工具算好、再把 xpub 貼進 `config.yaml`，不是即時串接的後台表單）。**Tron/EVM 鏈完全是另一套架構**：商戶用瀏覽器錢包（TronLink/MetaMask）直接連接一個商戶自己握私鑰的「主錢包」帳戶，輸入歸集目的地（冷錢包地址，規定須與主錢包不同），按下部署後在錢包簽名確認一筆鏈上交易——**主錢包部署一個智能合約，由合約負責批次生成充值地址**（「PayRam 生成一批充值地址，在客戶到達前處於未分配狀態」），主錢包需持有原生代幣（Tron上即TRX）支付部署與後續交易的手續費。也就是說 PayRam 用**智能合約**繞開了「帳戶模型鏈不支援匯出xpub」的限制，而不是像我們一樣硬用BIP44標準路徑做HD衍生。
  - 注意：這只是從官方文件片段查到的架構線索，**CREATE2/deterministic address、合約掃清（sweep）機制、gas精確處理方式、合約升級/重放風險等技術細節都沒有深入驗證**，不能當作我們可以直接照搬的完整方案，僅供架構方向參考。
  - 對我們的啟示：我們原本一直在細修的「xpub取得方式方式1（既有xpub）」在市場上本來就是稀有情境（連做法最貼近我們的PayRam，Tron這端都沒有真的用xpub），這是促成後續深入市調、最終決定廢除方式1的第一條線索（見下方另兩則市調筆記與上方「曾考慮但排除」清單）。
- **但開源圈子裡沒找到跟我們完全一樣做法的**：實際翻原始碼確認——
  - **SHKeeper**（`vsys-host/tron-shkeeper`）：每個地址是**伺服器生成的全新隨機私鑰**，加密後存資料庫，付款/歸集時解密使用——私鑰會經過伺服器，跟我們「伺服器全程不碰私鑰」是不同等級的安全設計。
  - **UPay**（`UPay-USDT/USDT-Payment-Gateway`）：**固定地址池輪詢分配**給訂單，不是 xpub 無限衍生，這正是進度.md 上面「曾考慮但排除」筆記裡提過的替代方案，證實市場上真的有人這樣做。
  - 結論：我們的 xpub-only 架構比市場上查到的開源方案都更嚴格，可能因為「這件事本身很難做對」+「不是 Tron 生態原生慣例（見下）」+「多數開源專案圖省事」三個原因疊加，不代表這條路有問題（六項技術驗證已用真實測試網廣播＋獨立查鏈證實可行）。
- **Tron 官方原生的多帳戶衍生慣例（TIP-01：`m/44'/195'/x'`，三層全硬化）跟我們用的 BIP44 標準五層路徑不同**——TIP-01 沒有非硬化 change/index 層，無法只靠 xpub 衍生多個地址，這解釋了為什麼「主流 Tron 錢包普遍不支援匯出可用 xpub」：不是市場剛好沒做，是 Tron 原生慣例的技術模型跟「xpub 衍生」這件事本身不相容。我們用標準 BIP44 五層路徑是滿足「伺服器不碰私鑰」需求唯一可行的選擇，不是選錯。
- **帳戶模型鏈（Ethereum、Tron）普遍不支援匯出 xpub，UTXO 鏈（Bitcoin）普遍支援**：查證 MetaMask（以太坊最主流錢包）也不支援匯出 xpub，跟 Tron 錢包狀況一致，是整個帳戶模型鏈家族的通例，不是 Tron 特例。
- **USDT 在 Tron 上是主流中的主流**：Tron 上 USDT 市值約 890 億美元、佔全體 USDT 市值 47%（所有鏈最高），小額（<$1000）零售支付市佔更高達 52%，需求書鎖定 USDT-TRC20 這個切角選對了市場。
- **Binance Pay / Bitfinex Pay 不是好的參考架構**：兩者主要走內部帳本（off-chain ledger），只有同平台會員互轉才免手續費，跟「開放式接受任何外部錢包付款」是不同題目，不適合拿來比對我們的收款地址設計（雖然 Bitfinex Pay 有支援外部錢包付款，但公開文件沒寫清楚那條路徑的地址怎麼產生，沒有深入查證）。
- **市場上唯一真實存在過的「Tron xpub HD衍生收款」實例已停運5年，且用非標準路徑**：`go-faast/tron-payments`（已停運交易所Faast的自用套件，`@faast/tron-payments`，libraries.io顯示最後發布約5年前，GitHub已封存）用Bitcore實作BIP32/44，路徑`m/44'/195'/0'/0/n`（非硬化change/index層），架構與我們規劃的完全一致。但這條路徑跟TronLink、imToken等官方錢包走的TIP-01（全硬化）路徑不相容——商戶若拿官方錢包助記詞給這套邏輯衍生，算出的地址官方錢包自己看不到、對不上帳，這正是此類方案在市場上幾乎絕跡的原因：不是密碼學做不到，是跟主流錢包生態兜不起來。Tron官方自己有提案想做這件事（[TIP-102](https://github.com/tronprotocol/tips/blob/master/tip-102.md)，路徑刻意設計成非硬化change/index層），但2019年提出至今仍是"Last Call"狀態、無任何錢包或交易所實作。業界對此的標準表述是「帳戶模型鏈（Tron/EVM）只需要地址，UTXO鏈才需要xpub」。
- **BIP32非硬化衍生有已知的洩露風險，但這是整個BIP32/44生態的通例，非我們架構特有**：BIP32官方規範明文警告「知道父層xpub加上任何一個非硬化衍生的私鑰，等同於知道父層完整的xprv（因此該父層底下過去和未來所有的公私鑰都會曝光）」，這是純橢圓曲線代數性質，跟鏈是UTXO還是帳戶模型無關；Tron官方TIP-01全硬化路徑正是在落實這條BIP32官方建議。但**這個風險不構成放棄xpub衍生架構的理由**——Bitcoin主流硬體/軟體錢包（Ledger、Trezor、Electrum）同樣是「帳戶層以上全硬化、change/index層非硬化」的結構，全球用戶每天都活在同一個風險假設下。光有xpub推不出任何私鑰，要出事必須**另外**發生一次子私鑰外洩的獨立事故（簽名環境中毒、惡意腳本、備份外流），不是正常操作會自動觸發的事。真正值得留意的是「歸集簽名環境的資安等級」——我們的5.9手動歸集是在商戶一般瀏覽器裡衍生私鑰簽名，風險等級接近Electrum這類軟體錢包（而非Ledger/Trezor等硬體錢包，私鑰衍生在晶片內完成、外洩面更小），但這仍是非託管錢包生態普遍接受的等級，不算特例。我們既有的「離線打包、嚴格CSP、無第三方腳本」規格，正是在對這個風險做緩解，架構設計階段應持續強化，但不需要因此放棄xpub-based架構。
- **沒有現成的瀏覽器插件能幫我們做掉「助記詞→xpub」這件事**：查證TronLink等錢包插件注入API（`window.tronLink`/`tronWeb`），只暴露「地址+簽名」方法，沒有任何方法能建立帳戶或匯出xpub/公鑰，協定設計上就不開放，程式化呼叫也繞不過去（[TronLink Docs](https://docs.tronlink.org/plugin-wallet/active-requests/)）。WalletConnect類協定規格同樣沒有xpub這個概念，本質是「請求簽名」不是「請求金鑰資料」（[WalletConnect Specs](https://specs.walletconnect.com/2.0/specs/clients/sign/namespaces)）。唯一「有機會」的路是Ledger硬體錢包WebUSB/WebHID直連瀏覽器，`@ledgerhq/hw-app-trx`在特定參數下會回傳`publicKey`+`chainCode`（私鑰不離開裝置），理論上可自行拼出xpub，但仍要自己寫BIP32序列化邏輯，且前提是商戶要有Ledger硬體，商業上不能取代現有方案。**結論：維持自建離線頁面路線，沒有插件可以省下來**。
- **iancoleman/bip39這類獨立BIP39/BIP32開發者工具，不建議讓商戶跳轉過去操作再貼回來，但可以引用其MIT授權的衍生邏輯**：查證iancoleman/bip39（純前端單一HTML+JS檔案，MIT授權，內建Tron/coin type 195選項，可直接輸出xpub，功能上完全符合需求）。但這工具因知名度高，**多次被釣魚仿冒**（GitHub issue [#437](https://github.com/iancoleman/bip39/issues/437)、[#556](https://github.com/iancoleman/bip39/issues/556)、[#444](https://github.com/iancoleman/bip39/issues/444)、[#269](https://github.com/iancoleman/bip39/issues/269)，假網域透過廣告排名超前真站，已有使用者助記詞被盜的真實案例）；若讓商戶跳轉到外部網站操作，等於把「商戶會不會跑錯網域」這個風險完全交給商戶自己判斷，比我們自建「離線打包+嚴格CSP」（助記詞永遠只在商戶已信任的自家系統網域下運作）安全等級明顯更差；且xpub是100+字元字串，複製貼上容易出錯，商戶也可能選錯幣別。**MIT授權允許自由修改再散布商用**，建議架構設計/開發階段直接把iancoleman/bip39處理BIP32/Tron xpub衍生的核心邏輯引入自己的離線打包+CSP頁面，同時拿到「被廣泛驗證過的程式碼」與「網域自主可控」兩個優點，優於完全自己從零刻或叫商戶跳轉外部工具。

### 下一步

1. 重啟階段 03 技術架構設計文件（技術選型已於下方確認，不受影響）
2. amd64 架構三項既有驗證都還沒測過，若正式環境非 arm64，建議在階段 04 骨架建置前補驗
3. 若正式採用 Go，注意 Go 版驗證項目 2 發現的簽名雜湊細節（Keccak256 vs SHA256），繞過高階 SDK 封裝自行處理簽名時要特別小心——驗證項目 6 在瀏覽器環境確認同一個坑會重踩，已記錄具體地雷（recovery byte 佈局、`prehash` 預設值）
4. 驗證項目 5 發現的「高風險頁面不可用 inline `<script>`、CSP 建議走伺服器端 HTTP header」這兩點，架構設計階段撰寫前端頁面規範時要納入
5. 驗證項目 6 發現的「廣播端網路層失敗不代表交易未上鏈」「TronGrid 餘額查詢有索引延遲」兩點，5.9 手動歸集頁面的架構設計要納入（先查鏈上狀態才能允許重簽/重廣播，避免重複轉出）
6. 驗證項目 7 發現的「IndexedDB 在清除瀏覽器資料/無痕模式/換裝置時會遺失」這點，5.9 本機加密儲存的 UX 設計要納入：需明確告知商戶這只是「省得每次輸入」的便利功能，不能取代助記詞本身的備份責任；PBKDF2 迭代次數等正式安全參數也待此階段拍板（驗證用的是 300000，非最終建議值）
7. 驗證項目 8 確認的架構原則：手動歸集頁面／TRX 手續費頁面凡是需要對外呼叫 TronGrid 的動作（組交易查詢鏈上狀態、廣播）一律由後端同源代理，瀏覽器不直連 TronGrid 網域，才能套用最嚴格的 `connect-src 'self'`；架構設計階段需明確決定「組交易」這步驟由前端或後端發起（驗證項目 8 採兩者皆走後端代理的保守設計，需求書本身未強制規定）
8. 主要營運/監管地區尚未拍板，原列於需求書第8節「待確認事項」，該節已整節移除（需求書不該有「尚未定案」的內容，只留已拍板決定），改在此追蹤：若未來明確鎖定特定地區，回頭確認是否需要在需求書第7節免責聲明補充對應用詞
9. EULA/銷售合約條款需另請律師審閱：原列於需求書第7節標題括號提醒，一併移除（需求書標題不該掛動作指示），改在此追蹤待辦
10. v0.33 討論 CONFIRMATION_STALLED 改判路徑時發現的技術提醒：訂單「累積金額」（5.2 金額判斷）在鏈重組（reorg）導致某筆入帳交易消失的情境下，計算方式需能反映最新鏈上真實狀態——若採「即時重新查鏈計算」，消失的交易自然不會被算入，不需額外處理；若採「只增不減的累加計數器」，需額外設計偵測交易消失後的金額扣回邏輯，否則累積金額會卡在一個實際已不存在的數字上。架構設計階段選擇實作方式時需納入這個風險。
11. 第6節前台收銀頁模組審查時發現的措辭缺口：OVERPAID/EXPIRED 訂單的收銀頁上，是否要提示付款人「已收到部分／超額款項，請聯繫商戶處理」，需求書 5.4 未明講，屬待拍板的介面文案方向，非架構層級決策，待介面設計階段一併處理。
